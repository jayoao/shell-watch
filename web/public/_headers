# Vite 會在檔名裡放內容雜湊，所以可以永久快取。
/assets/*
  Cache-Control: public, max-age=31536000, immutable

# ⚠ meta.json 是版本的來源，一定要每次重新驗證。
#   它被快取住的話，前端帶的 ?v= 會一直是舊版本號，等於整套破快取機制失效。
/data/meta.json
  Cache-Control: no-cache

# 分片與危害型態字典的檔名**沒有內容雜湊**（分片編號是 FNV-1a 算出來的，
# 資料更新後檔名不變）。改成由前端帶 `?v=<meta.version>`，
# 所以「同一個網址」的內容不會變，可以永久快取。
#
# ⚠ 以前是 max-age=3600，理由寫的是「資料更新最多一小時內生效」。
#   那個理由低估了風險：分片的**欄位格式**一改（例如得獎欄位從 5 個元素
#   變成 6 個），舊分片配新程式不是「資料舊」，是**每一欄往前移一格**。
#   2026-09-14 實測畫面顯示「有效至 1」而正確是「有效至 民國 117/10/12」，
#   而且不會報錯。所以改成版本化 + 永久快取，不要靠時間賭。
#
# ⚠⚠ 這裡**不可以**寫成 /data/*。Cloudflare Pages 會把所有比對到的規則
#   **合併**，不是只取最specific 的那一條 —— 2026-09-14 實測 meta.json
#   的回應標頭是
#       Cache-Control: no-cache, public, max-age=31536000, immutable
#   no-cache 與 immutable 互相矛盾，瀏覽器怎麼處理沒有保證。
#   目前是靠前端 fetch 的 { cache: "no-cache" } 擋住，但那只擋瀏覽器這一層。
#   所以改成逐一列出真正要永久快取的路徑，讓 meta.json 一條規則都不沾。
/data/c/*
  Cache-Control: public, max-age=31536000, immutable

/data/x/*
  Cache-Control: public, max-age=31536000, immutable

/data/hazards.json
  Cache-Control: public, max-age=31536000, immutable

/*
  X-Content-Type-Options: nosniff
  X-Frame-Options: DENY
  Referrer-Policy: no-referrer
  # ⚠ 不要讓搜尋引擎收錄。見 robots.txt 的說明。
  X-Robots-Tag: noindex, nofollow
