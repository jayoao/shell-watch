# ⚠ 這個網站不要被搜尋引擎收錄。
#
# 理由：畫面上會出現真實的公司名與真實的負責人姓名。
# 那些資料本身是勞動部依法公告的公開資訊，我們只是換一個軸線呈現 ——
# 但「被 Google 收錄」跟「在政府網站上查得到」是兩件事。
# 一旦被索引，某個人的姓名會永久跟「違反勞動法令」綁在搜尋結果上，
# 而本系統明確拒絕做「這些公司是同一人」的認定。
#
# 評審、老師、任何拿到網址的人都看得到，只是 Google 不收。
# 這是成本幾乎為零、但確實有意義的一道防線。
User-agent: *
Disallow: /
