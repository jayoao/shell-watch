# CLAUDE.md — 換殼追蹤

2026 第 31 屆 InnoServe。主投 LaborOD（勞動部職安署），副投 GCIS-OPENDATA（經濟部商發署）。
報名截止 10/05 · 初賽 10/13–23 · 決賽 11/07。

---

## 這個專案在做什麼

勞動部的違法雇主查詢系統**以公司為單位**。老闆解散公司換名字重開，違法紀錄就留在舊公司。
我們把軸線換成「人」：從一家公司 → 它的負責人與董監事 → 這些人名下的其他公司 → 那些公司的違法紀錄。

**核心技術問題**：兩邊資料沒有共同識別碼。勞動部只有公司名與負責人姓名，**沒有統一編號**。
只能靠姓名對接，而「陳志明」在全台有 467 家公司。
連 g0v 的公司資料網站都在注意事項寫「若要確認是否為同一人請進一步至經濟部商業司查詢」。

**把這一步自動化並量化可信度，就是本專案的貢獻。**

---

## 工作規則（踩過的坑，不要重踩）

### git

- **不要幫我 push。** 改動放在資料夾裡，然後給我：① 一句話說明改了什麼 ② 要 `git add` 哪些檔案 ③ commit 訊息 ④ 要不要跑 migration。
- 查狀態用 `git --no-optional-locks status`。一般的 `git status` 會留下 `.git/index.lock`，而這個環境刪不掉，會擋住我的 `git add`。
- 不要用 `git add -An` 之類的 dry-run，一樣會留 lock。

### 密鑰

- `.env` **永遠不進 git**。`.gitignore` 要有 `.env` / `.env.*` 但保留 `!.env.example`。
- **密碼、API key、連線字串不要貼進對話。** 貼過就視為已外洩，立刻更換。
- 雲端環境變數填連線字串時**不要加引號**。

### Python 環境（Windows）

- **一定要用 venv。** 直接用系統 Python 的 `pip install`，在 Windows 上很容易裝到
  另一個直譯器去（Microsoft Store 版、Anaconda、多版本並存），然後
  `python -m crawler.mol` 跑起來說找不到 requests。
  ```
  cd D:\class-project\shell-watch
  py -3.12 -m venv .venv
  .venv\Scripts\activate
  python -m pip install -U pip
  pip install -r requirements.txt
  ```
  之後每次開新終端機都要先 `.venv\Scripts\activate`（提示字元前面會出現 `(.venv)`）。
- **Python 要 3.10 以上**（用了 `X | None` 的型別寫法）。
- **`python -m crawler.mol` 一定要在專案根目錄跑**，不然是 `No module named crawler`。
- **主控台編碼**：Windows 預設 cp950，印不出 `⚠` 這類符號會直接 UnicodeEncodeError
  掛在半路。每個進入點都呼叫 `common.use_utf8_stdout()` 擋掉了，新寫的進入點也要加。
- `.venv/` 不進 git。
- **HTTP header 不能放中文。** header 只能是 latin-1，放了中文會在
  `http.client.putheader()` 掛掉，訊息是 `'latin-1' codec can't encode characters`，
  完全看不出跟 User-Agent 有關。踩過一次了（`crawler/mol.py` 的 `USER_AGENT`）。

### 前端

- **驗證用 `npm ci`，不要用 `npm i`。** 用 `npm i` 可能裝到跟 lockfile 不同的版本，然後出現「本機好好的，部署就壞」。上一個專案在這裡爆過：本機是 @types/react 19，Render 的 `npm ci` 裝 18，`RefObject<T | null>` 型別不相容，build 直接掛。
- **不要在 Windows 專案資料夾裡從 Linux 跑 `npm install`。** rollup 有平台專屬的原生檔案，Linux 裝的在 Windows 上跑不動（反之亦然）。要驗證就在乾淨的暫存目錄跑。
- **hooks 絕對不能放在 early return 之後。** 兩次渲染的 hook 數量不同 → React error #310 → 整頁白屏，而且 TypeScript 檢查不出來。`eslint.config.mjs` 已經把 `react-hooks/rules-of-hooks` 設成 error 並納入 `npm run build`。
- **顏色一律用 `var(--xxx)`，禁止寫死色碼。** 唯一來源是 `web/src/styles/tokens.css`。
- **資料從 JSON import，不要寫死在元件裡。** 真資料上線時只換檔案。

### 資料

- 爬下來的原始檔**先原樣存檔**（`data/raw/{單位}_{法規}.csv`），不要邊爬邊處理。爬蟲掛掉時不用重來。
- **用 DuckDB，不要用 pandas 全載入。** g0v 打包資料 2.4 GB，解開後好幾 GB。
- ⚠ **但寫進 DuckDB 要用 DataFrame 批次，不要 `executemany` 一列一列塞。**
  DuckDB 是欄式分析資料庫，逐列 INSERT 是它最差的情況。實測 20,000 列：

  | 作法 | 速率 | 300 萬列推估 |
  |---|---:|---:|
  | PRIMARY KEY + executemany | 343 列/秒 | **146 分鐘** |
  | 拿掉 PRIMARY KEY | 1,490 列/秒 | 34 分鐘 |
  | 拿掉 PK ＋ DataFrame 批次 | 233,697 列/秒 | **13 秒** |

  差 680 倍。DuckDB 會把本地的 DataFrame 直接當表讀（replacement scan），
  所以 `INSERT INTO t SELECT * FROM df` 幾乎零成本。
  **PRIMARY KEY 也不要加** —— 去重在 Python 的集合做掉就好，
  PK 只是讓它每列再檢查一次唯一性。索引最後才建。
- ⚠ **查 DuckDB 只查需要的那幾筆，不要全表掃再丟進 Python。**
  `pipeline/refine.py` 第一版對 427 萬筆 entity 跑相關子查詢
  （`(SELECT count(*) FROM director WHERE id = e.id)`）再全部載入，
  被 OOM killer 砍掉 —— **exit 137，stdout 與 stderr 都是空的**，
  完全看不出發生什麼事。把需要的名單先寫進暫存表再 JOIN，差好幾個數量級。
  看到「什麼都沒印、exit code 137」就是記憶體被砍，不是程式沒跑。
- **長時間的迴圈要印進度。** 70 MB 的分片跑起來看起來就像當掉了，
  人會以為是壞了而中斷 —— 而它其實只是慢。
- 含真實姓名的資料**不進 git**（`.gitignore` 已擋 `*_review.csv`、`data/annotation/`）。

---

## 資料事實（2026-09-02 實地驗證，不是推測）

### 勞動部違反勞動法令查詢系統

`https://announcement.mol.gov.tw/`（2026-09-02 讀表單原始碼＋實際打過）

- **沒有 API**。但查詢與下載都是同一個 form 的 POST，可以直接打：
  - 查詢 `POST /` → HTML，五組表格各自分頁
  - 下載 `POST /Download/` → **CSV，完整結果，沒有分頁**
  → **不要爬 HTML 表格、不要處理分頁**，直接打 `/Download/`。這省掉九成的工。
- 必要參數：`_csrf_token`（先 GET `/` 拿）、`CITYNO`、`REGNUMBER`、`downloadType=3`
- **不能空查詢** → 按「單位 × 法規」跑迴圈（代碼表在 `crawler/constants.py`）
- **30 個單位**（後 8 個是中央機關與園區管理局，很容易漏）× **10 種法規** = 300 次
- ⚠ 偶爾回傳 **HTML 錯誤頁**，但狀態碼還是 200、content-type 還是寫 text/csv。
  **一定要驗內容**（`crawler/mol.py` 的 `_looks_like_csv`），重試就會過。
- ⚠ HTTP header 寫 `charset=ANSII`，是錯的，實際是 **UTF-8 with BOM**。
- ⚠ 下載檔的**欄位順序跟畫面不一樣**：畫面是「…處分字號、事業單位」，
  下載是「…事業單位、處分日期、處分字號」。事業單位在第 4 欄。以下載檔為準。
- ⚠ 每列資料的欄位數比標題列**多 1**（行尾多一個逗號）。
  用 `len(row) == len(header)` 過濾會把資料全部丟光。
- ⚠ 罰鍰金額是**帶千分位的字串**（`"20,000"`）。
- 日期：資料裡是 `115/08/05`，但查詢表單要填 `1150805`。兩種不一樣。

**畫面顯示的筆數 ≠ 下載到的筆數。**
台北市職安法畫面寫「最近 3 年 3,391 筆」，下載回來 **16,708 筆（103/10～115/08，12 年）**。
下載的是全部年份 —— 資料量比原本估的多 4 倍。

- 備註欄有**訴願結果**（例：「訴願駁回」）→ 可區分已確定與爭議中，是法律風險的重要防線

### ⚠ 職安法的地理欄位幾乎是空的（2026-09-02 實測，這件事改變了計畫）

職安法確實多三個欄位（罹災人數／發生日期／發生地點），但**填寫率極低**：

| 單位 | 職安法筆數 | 有發生地點 | 填寫率 |
|---|---:|---:|---:|
| 職業安全衛生署 | 43,437 | 92 | 0.2% |
| 桃園市 | 6,655 | 32 | 0.5% |
| 高雄市 | 5,825 | 2 | 0.03% |
| 新北市 | 4,474 | 30 | 0.7% |
| 台中市 | 3,269 | 43 | 1.3% |
| 台南市 | 600 | 3 | 0.5% |
| **台北市** | **16,708** | **0** | **0%** |
| 基隆市／新竹市 | 0 | — | 沒有任何資料 |

罰鍰金額也一樣不一致：桃園 99.8% 有填，台北市 **16,708 筆全部是 0**。

**後果：**
1. 「用發生地點畫職災地圖」做不出來 —— 全台可用的點大約只有 200 個。
   第三層退路（職安風險地圖）**不能靠這個欄位**。
2. 地圖要改用**雇主的商工登記地址**（g0v 有），標題要誠實寫
   「違法雇主登記地點」，不是「職災發生地點」。這對換殼追蹤反而更貼題。
3. 罰鍰金額**不能拿來跨縣市比較**，也不能當嚴重度指標。
   `pipeline/build.py` 把「0」與空值都當成「沒填」，不是「罰 0 元」。
4. **這個發現本身就是題材**：資料揭露品質的縣市落差，是可以寫進簡報的實證觀察。

### 商工登記

- 開發用 g0v 打包：`http://ronnywang-twcompany.s3-website-ap-northeast-1.amazonaws.com/index.html`
  （資料日期 2026/07/20）
  - `index.csv.gz` 50MB —— 只有三欄 `id,type,name`，是**名稱 → 統編的索引**，
    type 分「公司」「商業登記」「分公司」
  - `files/` 1.14GB —— 但 `.json.gz` 與 `.jsonl.gz` 是同樣內容的兩份，
    **只要抓 20 個 `.jsonl.gz`，共約 560MB**
    - 前 10 個是公司與分公司
    - `bussiness-` 開頭的 10 個是商業登記（獨資、合夥）
      ← **換殼追蹤最relevant的一群**，資本額門檻低、開關容易
  - `gcis-history/` 1.16GB、`tax-history/` 64MB —— 目前用不到

### 商工登記載入後的實測數字（2026-09-03，`data/gcis.duckdb`）

    entity 4,267,888 筆　director 1,776,537 筆　（101 秒）
      商業登記 2,256,861　公司 1,930,265　分公司 80,762

⚠ **狀態欄位有全形與半形兩種寫法**：「歇業／撤銷」123,598 筆、
  「歇業/撤銷」52,008 筆。只認一種會**靜靜地**把 17 萬筆歸到「還在營業」。
  `clean_status()` 已經統一成半形。

⚠ **停業 ≠ 解散。** 「核准停業」31,608 筆是暫時的，復業的公司不是換殼。
  `CLOSED_STATUS`（永久）與 `SUSPENDED_STATUS`（暫時）分開列，不要混。

⚠ **設立年份有 2821 這種值。** 不擋範圍的話「設立未滿 N 年」會整個歪掉。

⚠ **合夥商號的出資額是 `{姓名: 金額}` 的 dict**，不是字串。
  當成字串處理會把資本額丟掉；而且 key 就是合夥人名單，是免費的第二份來源。

未遮罩的 79,883 筆幾乎都是**分公司經理**（不是負責人），
所以「負責人姓名一律遮罩」這個結論成立。
- API：`/api/show/<統編>`、`/api/name?q=<姓名>&page=1`
- ⚠ **打包檔的姓名全部遮罩，API 沒有遮罩。** 這是 2026-09-02 才查清楚的，
  之前寫「沒有遮罩」是錯的 —— 那次是用 API 查台積電，不是看打包檔。

  | 來源 | 百爵產業有限公司(20386468) 的代表人 |
  |---|---|
  | S3 打包檔 | `黃_斌`（實測該分片 1,926 筆遮罩率 **100%**，有限／股份公司都一樣） |
  | g0v API | `黃哲斌` |

  **所以兩邊分工**：登記地址、資本額、設立日期、登記現況、所營事業用打包檔
  （沒有遮罩問題、一次抓完）；完整姓名用 API（有量的限制，只用在候選名單）。
  高價值的三件事 —— 地址佐證、大企業排除、公司是否已解散 —— **完全不需要 API**。

  遮罩的名字也不是廢的：「黃_斌」保留了姓與末字，
  拿來比對勞動部的「黃哲斌」是很強的部分驗證，而且免費。
- ⚠ **財政部的資料只給「負責人姓氏」**（例如「仝」），一律用經濟部的
- 官方 API 要 email 申請並**綁外部 IP**，有每日上限 → 開發用 g0v，正式文件引用官方

### 已知資料限制（寫文件時要誠實揭露）

1. **各縣市保存期間差很多**：台北市勞基法涵蓋 12 年以上，有些縣市不到 2 年，
   基隆市與新竹市的職安法**一筆都沒有**。
   那不是雇主比較守法，是資料沒公開。→ 跨縣市比較一律用「期間內的率」，
   依據是 `python -m crawler.mol` 產生的 `data/coverage.csv`。
2. **沒有統一編號**，只能靠姓名對接。
5. **欄位填寫率因縣市而異**（見上一節）。沒填 ≠ 0。
3. **無法確認身分**：公開資料沒有身分證字號。我們**不宣稱能確認**兩個「王小明」是同一人，
   只量測「這個連結有多少獨立佐證」。
4. **系統偏保守、會漏抓** —— 這是刻意的：漏抓一個案例，使用者少收到一個警示；
   誤指一個沒問題的雇主，是實質傷害一個人。兩種錯誤的代價差好幾個量級。

---

## 架構原則

### AI 負責理解與連結，規則負責判定

- **LLM 只用在一個地方**：違法情節的嚴重度分級（`違反法規內容` 是自由文字）。
- **解析用 regex 就夠**（`pipeline/parse.py`），不要用 LLM —— regex 可驗證、免費、可重現。
  用職安署 43,437 筆實測，雇主欄有**五種格式**，不是一種：

  | 格式 | 佔比 | 例 |
  |---|---:|---|
  | 公司(負責人) | 44.97% | 七福營造有限公司(官建昌) |
  | 只有機構名 | 44.64% | 脇酩安全工程有限公司 |
  | 某某即某商號 | 6.29% | 王宇傑即衍寶企業社 |
  | 自然人雇主 | 2.34% | 方信和 |
  | 各種邊角 | 1.76% | 見 `tests/fixtures/hard_employers.txt` |

  一開始只寫括號那一條，成功率 37%，看起來像資料很髒 —— 其實不是。
  而且**「即」字與自然人雇主這兩種對換殼追蹤最有價值**：
  獨資商號與自然人雇主本來就最容易換個名字重新登記。當成解析失敗丟掉，
  等於把最該追的對象丟掉。現在整體約 99.8% 拆得開，
  拆不開的（多名共同雇主、括號沒配對）一律進 `unparsed.csv` 交人工，不硬猜。

  ⚠ **踩過的坑：沒有姓氏表就分不出人名與店名。**
  「外省牛肉麵」「轉角鍋物」「街口飯糰」「醇品咖啡屋」都是 3–5 個中文字、
  不以「公司」結尾 —— 跟人名長得一模一樣。`外省牛肉麵(陳木元)` 因此被判成
  「負責人＝外省牛肉麵」的自然人雇主：真正的負責人被丟掉，而一個不存在的
  「人」進了關聯圖。真實資料裡這種假姓氏有 813 種。
  現在 `parse.py` 有姓氏表，**但只用在沒有括號的情況** ——
  括號裡的位置本身就說明它是負責人，那裡不查表，否則 奥田実（日籍）、
  禤惠儀（罕見漢姓）這些真的負責人會被誤殺。

  ⚠ **括號裡也可能是機構屬性。**「…老人長期照顧中心(養護型)」的「養護型」
  在全台出現幾十次，會被當成「同一人名下 13 家公司」的重大發現。

  兩種錯的代價不對稱，所以判不出來時一律當機構：
  多一個機構名 = 一筆紀錄的欄位怪怪的；多一個「人」 = 憑空長出假的跨公司關聯。
- **風險分數、門檻、要不要顯示，全部是規則。** 理由不是保守，是這件事會影響公司名譽，
  判斷過程必須逐條可稽核。這句話要寫進簡報。

### 兩階段，刻意降風險

```
階段一（保證做得出來）  違法名單內部 → 同負責人多公司 → 慣性違法雇主圖譜
階段二（加分，可能失敗）跨機關 join → 他「現在」還經營哪些公司
```

階段一先做完再碰階段二。最壞情況也一定有東西可以交。

### 三層退路

若 M0 衝刺發現換殼案例太少：

1. 換殼追蹤（跨部會）→ 2. 慣性違法雇主圖譜（單源）→ 3. 職安風險地圖（不需實體解析）

第三層其實最貼組別主題（職安署贊助、組名有「視覺化」）。**三層都用得到隊友的地圖頁。**

---

## 身分訊號 vs 樣態訊號（不要混在一起）

2026-09-03 用商工登記的**同地址**當獨立量尺，檢驗每個訊號實測出來的：

| 訊號 | 類型 | 同地址比例（符合 vs 不符合） | 倍數 | 顯著 |
|---|---|---|---:|:--:|
| 可信度分數 ≥ 3 | 身分 | 6.69% vs 2.06% | 3.24× | ✓ |
| 可信度分數 ≥ 5 | 身分 | 8.69% vs 3.40% | 2.55× | ✓ |
| 裁處單位都是同一縣市 | 身分 | 4.54% vs 3.05% | 1.49× | ✓ |
| 先前的公司都已停業 | 樣態 | 3.70% vs 3.79% | **0.98×** | － |
| 只有 2 家公司 | 樣態 | 3.83% vs 3.44% | 1.11× | － |
| 資本額都低於 100 萬 | 樣態 | 3.31% vs 3.88% | 0.85× | － |

**「先前的公司都已停業」是 0.98 倍 —— 完全沒有效果。**
第一直覺會說「這是換殼的定義性特徵，怎麼可能沒用」。
它確實有用，但**回答的是另一個問題**：

- **身分訊號**（姓名稀有度、同縣市、同地址）→ 這幾家公司是不是同一個人的
- **樣態訊號**（已停業、時間依序、組織型態）→ 這個樣子像不像換殼

同地址量的是身分，所以量不出樣態訊號的價值。而且全國本來就有 **55%**
的登記處於解散／歇業狀態，那個訊號接近基準率，對身分判斷沒有資訊量。

**排序要分兩層做：先用身分訊號決定連結可不可信，再用樣態訊號決定是換殼還是集團。**
把兩種訊號加進同一個分數，等於把兩個不同的問題混成一個答案。

## 最終排序是兩層（pipeline/rank.py）

身分層只用**驗證過**的身分訊號分級，樣態層沿用 `shell.py` 的分類。
分層只用「姓名稀有度」與「同縣市」，**同地址不參與分層**，
留下來當獨立的校準尺 —— 這樣校準才不是循環論證。
同地址本身是最強的單一證據（274 倍），所以它的作用是**直接把該組升到 A 層**。

實測校準（同地址比例）：

| 層級 | 組數 | 同地址 | ±2SE |
|---|---:|---:|---:|
| A 姓名罕見＋同縣市 | 220 | **11.36%** | 4.28 |
| B 姓名罕見 | 3,048 | 4.46% | 0.75 |
| C 同縣市 | 162 | 7.41% | 4.12 |
| D 兩者都沒有 | 5,230 | **1.40%** | 0.32 |

A > B 顯著、C > D 顯著、**B 與 C 分不出來**（C 只有 162 組，2SE 是 ±4 個百分點）。
所以能講的是 **A > {B, C} > D**，不要宣稱 B 一定強於 C。

⚠ **比較相鄰兩層一定要看樣本數。** 第一版寫成「比例沒有隨層級遞減就判定分層失敗」，
把 C 層的雜訊當成錯誤。這個坑在這個專案已經踩過三次
（refine.py 的門檻挑選、rarity.py 的分層判定、這裡），一律要跟 2SE 比。

⚠ `join.py` 的輸出**必須帶 `county_only`**。少了它，`rank.py` 分不出
「同一個縣市」與「兩家都是勞保局裁處」，A 層與 C 層會變成 0 組，而且不會報錯。

## 大企業排除：商工登記幫不上忙（實測結論）

用資本額、設立年、董監事人數做排除，**七個門檻沒有一個的效果超過 2 個標準誤**。
`pipeline/shell.py` 的四個訊號已經把大企業處理掉了，再篩沒有剩餘價值。

第一版的作法是「挑出讓同地址比例上升的條件」，會砍掉 51% 的候選，
換到 0.5 個百分點、而且在雜訊範圍內的「改善」—— 那是純粹的損失。
**同地址只有 3~4%，樣本一少比例本來就會跳動，一定要跟抽樣誤差比。**

兩個實作上的坑：
- `director` 表裡有「合夥人」（來自出資額 dict 的 key）。
  混著算，「董監事 ≥ 5 人」會去砍 21 萬資本額、9 個合夥人的小商行。
- 「設立滿 20 年」量的是**開了多久**不是**有多大**。
  實測被它砍掉的包括資本額 5 萬的餐飲企業社、100 萬的清潔公司 ——
  那正是我們要找的對象。

商工登記的價值在**佐證**（同地址 274 倍、登記現況），不在排除。

## 職安危害型態（pipeline/hazard.py、tools/hazard_check.py）

職安法 74,655 筆的違規描述有 34,650 種寫法，用規則歸成 15 類危害型態，
每一類附上「法規要求雇主做什麼」。**涵蓋率 95.0%。**

⚠ **涵蓋率不是正確率。** 一條抓錯東西的規則，涵蓋率一樣很漂亮。
所以有 `tools/hazard_check.py`，用**法條欄位**當獨立的尺：

    hazard.py 讀 violation（違規事實的白話描述）
    hazard_check.py 讀 law_article（裁處引用的法條）

兩個欄位由裁處機關分開填寫，邏輯完全不同。對照表**從法規本文來，
不是從資料來** —— 每一條都在全國法規資料庫查證過（2026-09-03），
從資料歸納對照表就不是獨立的尺了，那只是把同一件事量兩次。

實測 47,324 筆對得到明確法條的紀錄，**兩邊講同一件事的比例 94.3%（±0.2%）**。
主要幾類：

    營造標準 §17/19/20/22/42/56/59（護欄護蓋安全網）   18,062 筆   99.8%
    職安法 §25-28（承攬與共同作業）                    10,140 筆   99.7%
    營造標準 §11-1（安全帽）                           3,053 筆  100.0%
    設施規則 §281（二公尺以上安全帶）                    1,660 筆  100.0%
    設施規則 §57（機械掃除上油應停機）                    1,508 筆   97.1%

### 對不上的地方，錯的是法條欄位不是描述

第6條第1項的「款」對不上的比例很難看（第13款 20.7%、第4款 4.3%）。
進去看之後發現：**同一句一字不差的描述，在不同筆被引到不同款。**
只引一款的 11,894 筆裡有 31.9% 是這樣：

    「有墜落危險之處所未設防護設備。」  第13款 578 筆　第5款 532 筆

第13款是「通道、地板或階梯」，第5款才是「墜落」。這句話明明是墜落。
法條欄位在這一段不是一把穩定的尺 —— 不一致是它造成的。

這反過來說明為什麼要讀**描述**而不是只信法條欄位。
寫文件時這是資料品質的發現，不是拿來替自己脫罪的藉口。

### ⚠ 驗證工具出錯比被驗的東西出錯更危險

第一版 hazard_check 想抓「第3款」，寫成 `第[^款]{0,8}3[^0-9款]{0,4}款`，
結果把「第**13**款」也吃掉了。表上顯示「電、熱」只有 27.4% 對得上，
害我差點去改本來正確的分類器。

被驗的程式錯了，數字會變難看；**驗證工具錯了，會叫你去改對的東西。**
所以款號一律真的解析出來，不要用正規表示式硬湊。

### 只引一款時才拿來當尺

公告常寫「第1、3、5款」，那等於把三種危害全圈進來，對得上是理所當然 ——
拿那種列舉去算命中率是在自己給自己送分。`kuan_expected()` 只認單獨一款。

## 查詢頁的資料怎麼來（pipeline/publish.py）

查詢頁一度只能查 12 筆去識別化樣本。那不是產品，是簡報用的假畫面。

現在 `python -m pipeline.publish` 把**全部 175,898 家、628,893 筆**
切成 2,048 個靜態分片放進 `web/public/data/`。

    分片編號 = FNV-1a(norm_name(公司名)) % 2048

前後端各算一次同樣的雜湊，所以**不需要索引檔** ——
索引 175,898 個公司名本身就要好幾 MB，那才是真正的成本。
一次查詢只下載一片（原始平均 79 KB，gzip 後 14 KB）。

### 為什麼不用後端

不需要資料庫、不需要伺服器、不用付錢，而且**demo 當天不會因為後端掛掉而開天窗**。
考慮過 duckdb-wasm / sql.js-httpvfs（HTTP range 查遠端 SQLite），程式碼會少一點，
但多一個執行期相依。比賽現場的可靠度比程式碼行數重要。

### ⚠ 兩邊的雜湊必須永遠一致

`pipeline/publish.py` 的 `fnv1a()`／`norm_name()` 和
`web/src/lib/lookup.ts` 的 `fnv1a()`／`normName()` 改了一邊沒改另一邊，
症狀是**「有些公司查不到」而不是報錯** —— 看起來像資料沒部署好，其實是程式錯了。

所以 `publish.py` 會把 6 組真實公司名的分片編號寫進 `meta.json`，
前端載入時重算比對，不合就在畫面上顯示硬錯誤。
用真實公司名而不是固定字串，因為要連 `norm_name` 一起驗
（臺→台、全形括號、全形空白都在真實資料裡出現過）。

⚠ Python 的內建 `hash()` 有 `PYTHONHASHSEED`，每次執行都不一樣，**不能用**。
FNV-1a 是選它因為短到兩邊都不可能寫錯，而且沒有平台差異。

### 核心名別名：不能自己挑一家

沒有這一層，使用者必須一字不差打出「一允企業有限公司」才查得到。
現場沒有人會這樣打，他們會打「一允企業」，然後以為系統壞了。

所以砍掉結尾的組織型態字尾當成別名。**但字尾清單要保守** ——
砍太多會把不同公司併成同一個核心名，那不是查不到，是**查到別人**。
實測 1.8% 的核心名對到多家公司，一律讓使用者選，程式不准自己挑。

### ⚠ 正規化撞名要合併，不能覆蓋

「臺南紡織股份有限公司」與「台南紡織股份有限公司」`norm_name` 之後是同一個 key。
第一版寫成 `bucket[key] = entry`，直接覆蓋 —— 前一個寫法的 5 筆裁處紀錄整批消失，
使用者查到「這家只有 5 筆」，真相是 12 筆。

實測 90 組 key 撞在一起（180 個公司名），全部都是同一家公司的兩種寫法。
**查詢系統少報違規紀錄，是這個專案最不能犯的錯**，而且畫面上不會有任何異狀。
現在是 `merge_entry()` 合併，顯示名稱取紀錄較多的那個寫法。

### 產出不進 git

`web/public/data/` 在 `.gitignore` 裡。159 MB 的產生物進 git，
每次重新產生就是一次全量改寫，歷史會爆掉；而且它是衍生資料，
來源 `data/records.csv`、`data/ranked.csv` 本來就沒進 git（有真實姓名）。

**部署是本機產生、直接上傳**（Cloudflare Pages direct upload），不走 git。

### ⚠ 這份資料是真實姓名

查詢頁接上完整資料之後，畫面上出現的是真實公司名與真實負責人姓名。
截圖、影片、簡報一律改用 `python -m pipeline.export --sample 12` 的去識別化樣本。
前端沒有部署 `web/public/data/` 時會自動退回那份樣本，並在畫面上標示。

## 改完立刻 commit

這一條是踩出來的：`Home.tsx` 的查詢串接和 CLAUDE.md 的一整個章節，
都因為在工作目錄裡躺了兩個小時而被蓋掉（編輯器把舊的緩衝區寫回去）。
重寫歷史、renormalize、checkout 都會動工作目錄。

**寫完一個能跑的段落就 commit**，不要等到整批做完。

## ⚠ 前端一定要跑 eslint，不只 tsc

全形空白（`　`）直接寫在原始碼裡會被 `no-irregular-whitespace` 擋，
但 `tsc` 不會抱怨。同一個坑在這個專案踩過三次（Home.tsx 兩次、lookup.ts 一次），
第三次就是因為只跑了 `tsc`。要用 `\u3000` 或 `{"\u3000"}`。

## 部署（DEPLOY.md）

純靜態，沒有後端。`web/public/data/` 的 159 MB 分片**不進 git**，
所以不走 GitHub 自動部署 —— 本機產生、本機打包、`wrangler pages deploy` 直接上傳。
完整步驟、部署後的五項檢查、上線後的紅線都在 `DEPLOY.md`。

### ⚠ 上線的網站有真實姓名，一定要 noindex

`web/public/robots.txt` 與 `_headers` 的 `X-Robots-Tag` 都設了 `noindex, nofollow`，
`index.html` 也有 meta。**不要拿掉。**

資料是勞動部依法公告的公開資訊，我們只是換一個軸線呈現。但「被 Google 收錄」
跟「在政府網站上查得到」是兩件事：一旦被索引，某個人的姓名會永久跟
「違反勞動法令」綁在搜尋結果上，而本系統明確拒絕做「這些公司是同一人」的認定。

評審、老師、拿到網址的人都看得到，只是搜尋引擎不收。成本幾乎為零。

### 分片不能永久快取

`_headers` 給 `/assets/*`（Vite 有內容雜湊）永久快取，
給 `/data/*` 只有一小時 —— 分片的檔名**沒有雜湊**（編號是 FNV-1a 算的，
資料更新後檔名不變），永久快取會讓使用者查到舊資料。

## 人工標註與 LLM 分級的結果（2026-09-12）

### T7 違法嚴重度：kappa 0.873

兩個人各自獨立標同一份 150 筆（判準見《嚴重度標註判準.pdf》）：

    直接一致           135/148 = 91.2%
    Cohen's kappa      0.873          ← 目標 ≥0.61，這是「非常好」

最大的分歧是 **中度 ↔ 重大（7 組）**。那一格的界線本身就模糊，
不是誰標錯了 —— 判準要再細分的話就從那裡下手。

### LLM 分級：高信心一致率 92.0%

`pipeline/severity_llm.py` 是專案唯一使用語言模型的地方。

    整體一致率        130/148 = 87.8%
    高信心（n=138）   127/138 = 92.0%    ← 佔全部的 93.2%
    中／低信心（n=10）   3/10  = 30.0%    ← 送人工複核

高信心 vs 中低信心差 62 個百分點，合併 2SE 是 29.3% → **顯著**。
信心等級真的能認出自己會錯的地方，「送人工複核」不是裝飾。

### ⚠ 這兩個數字要一起寫，而且不能互相取代

    人 vs 人   → kappa 0.873   → 判準夠不夠清楚
    模型 vs 人 → 92.0%         → 模型能不能用

**模型在高信心那一批的表現（92.0%）與兩位人工標註者之間的一致程度
（91.2%）相當** —— 那是這個任務在現有標註品質下的天花板。
不是「模型還差一點」，是已經到頂了。

而且兩個人最常吵的那一格（中度↔重大）跟模型最常錯的那一格是同一格，
那進一步說明剩下的差異來自判準的模糊，不是模型的弱點。

### 信心等級是調出來的，不是一開始就對

第一版：整體 85.1%，高信心 86.2% —— **兩個數字一樣，等於信心等級失效**，
模型把 98% 標成「高」，認不出自己哪裡會錯。

逐筆看 22 筆不一致後發現：7 筆是同一條判準（承攬管理）沒被寫進提示詞、
1 筆是人工標錯、其餘是判準邊界。把判準補齊、並把「高」改成**需要理由**
（逐字對得上判準才給高，用了推論就給中）之後才變成 92.0% / 30.0%。

⚠ 快取的鍵要**算進判準的指紋**。第一版只算「法規＋法條＋描述」，
改了判準重跑會從舊快取拿結果 —— 改了等於沒改，而且看不出來。

## 地圖頁：登記地址，不是發生地點（pipeline/geo.py）

地圖頁本來要畫「職災發生地點」。**做不到** —— 職安法 74,658 筆裡
只有 189 筆填了發生地點（0.25%），全台可用的座標大約 200 個。

改成用事業單位的**商工登記地址**，聚合到鄉鎮市區畫比例符號圖：

    公司名 175,918 → 對到商工登記且有可用地址 147,560（83.9%）
                   → 解析得到鄉鎮市區 145,222（98.4%）
    職安法 64,997 / 74,658 筆上得了圖（87.1%），分布在 365 個區

⚠ **這兩件事不一樣，而且在營造業差很遠**（公司登記在台北、工地在桃園）。
  頁面上從標題、警語、圖例到每一個 Popup 都要寫「登記地址」。
  一張標示不清的地圖比沒有地圖更糟 —— 它看起來很專業。

### 一定要有分母

只畫件數的話，畫出來的是「哪裡公司多」——一張人口密度圖。
台北市中山區排第一（3,457 筆）只是因為全台最多公司把總部登記在那裡。

分母用商工登記裡**現存**（排除解散／歇業，全國 55% 是已停業）的家數：

    台北市中山區   3,457 筆 / 55,369 家 =  62.4 ‰   ← 件數第 1
    新北市五股區   1,647 筆 /  8,505 家 = 193.7 ‰   ← 率第 1
    彰化縣伸港鄉     195 筆 /  2,145 家 =  90.9 ‰   ← 其中 32 筆涉及死亡災害

分母 < 500 家的區不算率（幾十家的區裡一家被罰兩次就衝到全國第一）。
門檻是人訂的，畫面上要寫出來。率一律附 ±2SE。

### 篩選一改，畫面上每個數字都要跟著篩

這一頁踩到兩次同一個錯：使用者篩「只看感電」之後，

    合計 2,986 筆（感電） 其中 1,959 筆涉及死亡災害 · 57 筆行政救濟尚未終結
                              ↑ 全部型態的           ↑ 全部型態的

兩個數字各自都對，放在一起卻在回答一個沒有人問的問題，而且**看起來完全正常**。
所以 `geo.py` 除了 `haz`（每型態筆數）之外，還要輸出 `hazf`（每型態的死亡筆數）
與 `hazp`（每型態的行政救濟未終結筆數），前端一律用 `xxxOf(r)` 取值，
不要直接讀 `r.fatal` / `r.pending`。

規矩：**新增一個篩選條件時，把畫面上所有數字列出來，一個一個問
「它有沒有跟著這個條件走」**。答案是「沒有」的那些，不是補資料就是拿掉。

### 地址轉經緯度：四條路死了，第五條活了（2026-09-13）

使用者（和評審）看到區中心的圓點，第一個反應都是「為什麼不畫在真實位置」。
前四條路都走過，都不通：

    1. 職安法的「發生地點」欄位   填寫率 0.25%（74,658 筆只有 189 筆有值）
    2. data.gov.tw 門牌坐標資料   各縣市各自上架，**缺台中市與彰化縣**
    3. 國土測繪中心開放 API       實測：ListCounty ✓、座標→村里 ✓、地址→座標 **404**
    4. TGOS 全國門牌位置比對服務  技術上完全合用（每月 30 萬筆，我們只要 2.5 萬），
                                  但**申請對象不含「個人」**，學生申請不到

第五條通了：**taiwan-address-data**（BSD 授權，原始來源就是 TGOS，放在 GitHub），
一條路一個檔，含門牌級的 WGS84 座標。合法公開的釋出，不需要申請。
工具是 `tools/geocode.py`，產出 `data/geocode.csv`。

    門牌精確   18,261   74.7%
    退到路名    4,759   19.5%
    抓不到      1,421    5.8%

⚠ **這支要連 github.com，本機的 Linux VM 連不到，要在 Windows 的 venv 跑。**

### ⚠⚠ 定位失敗率一定要按縣市拆開看

第一版是 67.8% / 16.4% / 15.8%，總成功率 84% 看起來很漂亮。
但拆開之後，**失敗率是照城鄉分佈的**：

    台北市  抓不到  0.4%
    彰化縣  抓不到 47.6%   ← 伸港、線西（致死比例最高的兩個鄉）就在這裡
    嘉義縣  抓不到 71.4%

照那樣畫出來，都會區密密麻麻、鄉下稀稀落落，讀圖的人會得到
**「鄉下比較少違規」的結論，而那是定位失敗造成的假象**。

三個 bug 全在解析端，修完彰化掉到 6.9%：

    1. 「前鎮區」被斷成「前鎮」（鎮結尾）→ 剩下「區民權二路」抓不到
       修法：「區」要先試而且要貪婪，不能用非貪婪的 [區鄉鎮市]
    2. 來源寫「臺灣大道」「新臺五路」，我們的正規化把臺換成台 → 整條路抓不到
       修法：台／臺兩種變體都試
    3. 只剝「里」沒剝「村」—— 鄉下用村，村名被當成路名
       修法：`^.{1,4}?[里村]`　← 這一個是城鄉偏差的主因

**規矩：任何比對／定位的成功率，都要按一個跟結論無關的維度（縣市、年份、
法規）拆開看一次。** 總數漂亮而分佈有偏，比總數難看更危險。

### ⚠ 來源資料的髒點要在「取用前」濾掉

`geocode.csv` 第一版有兩個座標落在台灣外：

    嘉義縣…中洋工業區2之1號   lat 17.6   ← 菲律賓外海
    金門縣…安岐96-1號2樓      lng 4796012.86  ← TWD97 的公尺值混進經緯度欄位

問題不在那兩筆本身，在於**壞點被平均進「路名層級」的座標**——
汙染的是那一整條路上所有公司的位置，而且畫面上完全看不出來。
所以範圍檢查要放在「讀進來就濾」，不是「產出後檢查」。

### ⚠ 地圖底圖不要打外部圖磚伺服器

第一版用 `tile.openstreetmap.org`。本機開發完全正常，**部署上線之後**
整張圖變成一格一格的「Access blocked / 403」—— OSM 的圖磚是志工出錢跑的，
有使用政策，公開網站直接打會被封。

真正的問題不是 OSM，是它違反這個專案自己的原則：

    「不需要資料庫、不需要伺服器 —— 展示當天不會因為後端掛掉而開天窗」

地圖頁偷偷開了一個對第三方的依賴，而且它掛掉時畫面上是一排「Access blocked」，
比空白更難看、更像整個系統壞了。**換一家圖磚商只是把同一個風險換個名字。**

現在底圖是自己的資料：`web/src/data/tw_districts.json`，374 個鄉鎮市區界，
來源 g0v/twgeojson，Douglas-Peucker 容差 0.0025 度（約 250m），
45 萬個點簡化到 1.3 萬個（2.9%），288 KB／gzip 78 KB。
桃園縣的 13 個鄉鎮市已改成桃園市的區；丟掉 0.45 km² 以下的無人小島
（門檻要留得住連江縣東引鄉的 4.4 km²）。365 個有資料的區全部對得到界線。

**地圖頁的外部請求數是 0。** 離線可用。要加任何外部資源之前先問一次：
「展示當天它掛掉會怎樣？」

⚠ `web/src/pages/OshaMap.tsx`（她的逐筆版，已不在路由上）裡面還留著
  那個 TileLayer。要讓那一頁復活的話不能直接用，會踩同一個坑。

### ⚠⚠ 不要把 bounds 交給 MapContainer

react-leaflet 的 `bounds` prop 是在**建立地圖的那一刻**呼叫 `fitBounds` 的，
而那一刻容器往往還沒有正確尺寸。兩種後果都實際發生過：

    容器尺寸偏小  → 算出來的縮放偏高 → 正式站上只看得到北台灣
    容器尺寸是 0  → 除以零 → 中心點變 NaN
                  → 丟出「Invalid LatLng object: (NaN, NaN)」
                  → **整個 React 樹掛掉，整頁空白（不只是地圖）**

第二個是加了 `invalidateSize` 之後才浮出來的 —— 原本它只是靜悄悄地畫不出東西，
加上去之後變成明確的 crash。**那是好事**：靜悄悄的錯比會噴錯的錯危險得多。

正確做法：建立時給固定的 `center` / `zoom`（永遠算得出來），
真正的 `fitBounds` 交給 `KeepSized`，等容器確定有尺寸之後才做。

### ⚠⚠ Leaflet 的容器尺寸是快取的 —— 沒有 ResizeObserver 就是空白地圖

**這一個是整個地圖頁最危險的坑，因為它本機不會發生。**

Leaflet 建立地圖時量一次容器尺寸就快取起來。如果那一刻容器寬度是 0
（分頁在背景、視窗還在開、CSS 比 lazy chunk 晚到），它把裁切範圍算成 0 寬，
然後**每一個圖形的 `d` 都變成 `"M0 0"`**：

    DOM 裡 667 個 path 都在、293 個圓點都在、console 一個錯誤都沒有
    畫面上什麼都沒有

實際發生的順序：本機一切正常 → 部署到 Cloudflare → 整張圖空白。
而本機之所以「正常」，是因為開發時視窗被改過大小，觸發了 Leaflet 自己的
resize 處理把尺寸救回來 —— **等於開發過程把證據擦掉了**。

`OshaDistrictMap.tsx` 的 `KeepSized` 元件就是在修這件事：
ResizeObserver 盯著容器，每次尺寸變化 `invalidateSize()`，
第一次量到寬度真的 > 0 才 `fitBounds`（之後不再 fit，不然使用者放大到
某個區去看、一改視窗大小就被彈回全台）。

驗證方式（不要只看「本機看起來正常」）：

    把容器 maxWidth 改成 0px  → 有效 path 從 662 掉到 30、svg width=0
    改回 660px               → 自動回到 662、svg width=790

⚠ 判斷地圖有沒有真的畫出來，不能數 DOM 節點數 —— 要數
  `d` 屬性長度 > 20 的 path 有幾個。節點數永遠是對的。

### 沒有圖磚就沒有地名，所以縣市標籤要自己畫

拿掉 OSM 圖磚之後，畫面上一個地名都沒有 —— 使用者看不出自己在看哪裡。
`COUNTY_LABELS`：22 個縣市，落點是該縣市所有鄉鎮市區的**面積加權形心**。

⚠ 有三組要手動挪，因為「市」被「縣」包起來時兩個形心會疊在一起：
  新北市（台北市在裡面）、嘉義縣（嘉義市在裡面）、新竹縣（新竹市在裡面）。
  挪過的落點仍要在該縣市境內。

### 陸地要比海亮

深色主題下拿 `--surface`（#161F27）當陸地、`--surface-2`（#1E2A33）當海，
陸地比海暗 —— **整座島看起來像一個洞**。
淺色要「白陸地＋灰海」、深色要「灰陸地＋近黑海」，同一個 token 在兩個主題的
明暗關係剛好相反，湊不出來。所以 `tokens.css` 另外定義了
`--map-land` / `--map-sea` 兩個專用 token（顏色不寫在元件裡，維持原本的規矩）。

圓點顏色用 `--accent` 不要用 `--accent-fill`：深色主題下 accent-fill 壓在
深色陸地上對比不夠。

### 地圖的三個視覺陷阱（都實際踩過）

    寫死 zoom        容器 1,000px 寬時橫向塞進 11 個經度，本島只有 2.1 個
                     → 七成畫面是福建跟太平洋。改用 bounds 讓 Leaflet 自己算。
    zoomSnap 預設 1  本島在這個框裡卡在 zoom 7 跟 8 中間，只能取整數就退回 7，
                     等於沒改。要設 zoomSnap={0.25}。
    地圖撐滿版面      台灣是直的。地圖框限寬 660px，本島才會佔滿高度的九成。

圓點：半徑上限 10px、淡填色（0.22）＋明顯外框（1.4）、**大圓先畫小圓後畫**。
西部工業帶 300 個區一定會重疊，填得越實越糊；畫成圈圈才讀得出層次，
而且小區不會被鄰居蓋住點不到。

### 座標來源

`pipeline/centroids.json`：377 個鄉鎮市區的幾何中心，從 g0v/twgeojson 的
界線算的（取面積最大那一圈的形心，避免離島小島把中心點拉到海裡）。
桃園縣的 13 個鄉鎮市已改成桃園市的區。

⚠ 形心是「這個區的中間」，不是任何一筆紀錄的實際位置。

### 她原本的逐筆版本留著

`web/src/pages/OshaMap.tsx` 已不在路由上，但**不要刪**：
拿到有座標的資料就能復活，而且那是 T5 的工作成果，要留在 repo 當證據。
改地圖之前先問一聲，不要兩個人同時改同一份。

## 兩個絕對不能寫進簡報的數字

1. **build.py 的「名下有 2 家以上事業單位 N 個」** —— 那是**同名**的統計。
   「陳志明」在全台有 467 家公司。這個數字是上界，不是發現。
2. **build.py 的「各公司違法期間不重疊」** —— 稀疏資料下這幾乎是算術必然。
   兩家公司各只有一筆違法，只要不同天就一定「不重疊」。它不代表換殼。

能用的是 `pipeline/signal.py`：要求至少一家有 2 筆以上（span 是真的期間）、
兩段之間隔 90 天以上、名字 3 字以上，並且做**同名巧合檢定** ——
比較「這些名字的多家公司全在同一縣市」的實際比例與隨機期望值。
實際 >> 期望才代表訊號存在。

簡報能講的是「這批連結整體顯著偏離隨機（N 倍）」，
**不能講「我們找出了 N 個換殼案例」**。個別配對要人工標註 + Cohen's kappa。

## 可信度分數（pipeline/rarity.py）

把姓名拆成「姓 + 名」分別估機率再相乘，算出「照機率這個名字本來預期
會出現在幾家公司上（λ）」，再用 Poisson 上尾算實際看到 k 家有多不尋常：

    score = -log10( P(X ≥ k | λ) )

**為什麼要拆開姓與名**，有兩個理由，兩個都重要：
1. 直接數出現次數是循環論證 —— 我們要判斷的就是那幾次是不是巧合。
2. 拆開後，單一姓名對 P(姓)、P(名) 的貢獻只有幾萬分之一，可以忽略。

⚠ **踩過的坑：機率要用「每家公司一筆」估，不是「每個不重複姓名一筆」。**
   一開始寫成後者，P(名=明) 變成「有多少種名字含明」而不是「有多少人叫明」，
   常見姓名的 λ 被嚴重低估，於是「陳志明名下 29 家公司」被判成 99 分的
   重大發現 —— 而那正是 29 個不同的人。**分數把最不可信的組排到最前面。**
   λ = 公司數 × P(姓名)，P 是公司母體上的分布，估計就要在同一個母體上做。

⚠ Poisson 累加在 λ > 745 時 `math.exp(-lam)` 會下溢成 0，p 變 1.0、分數變 0，
   把該標注的組默默漏掉。常見姓名的 λ 真的會到幾百，所以 λ>500 改用常態近似。

**分數自己有沒有效，程式會驗**：分數越高的組，名下公司落在同一縣市的比例
應該越高。用合成資料（植入 1,500 個已知真案例）測過，趨勢是
6.8% → 8.5% → 13.7% → 15.5%，單調上升。**這張表就是簡報上「技術怎麼證明」那張圖。**

## 換殼 vs 集團（pipeline/shell.py）

rarity.py 的分數在量「這個關聯有多難得」，它做對了 —— 分數最高的幾組是
統一、遠東、統聯這些真實集團，系統在沒有任何先驗知識的情況下把它們拼了回來。
**但那是正確的辨識、錯誤的目標。** 求職者不需要知道統一超商的老闆也開統一企業，
而把徐旭東放在「求職安全雷達」首頁是災難級的誤導。

在接到商工登記（實收資本額、設立年份、董監事人數）之前，用四個手上就有的訊號分流：

| 訊號 | 集團 | 換殼 |
|---|---|---|
| 時間樣態 | 同時經營 | 先後接續 |
| 公司名 | 共用字根（統一〇〇、遠東〇〇） | 刻意改名，不相關 |
| 組織型態 | 股份有限公司 | 有限公司、企業社、工程行、獨資 |
| 公司數 | 8 家以上 | 2–4 家 |

判定用規則不用模型，每一條都要能說出理由（`why` 欄位）。
兩家都是股份有限公司又只有兩家時，判「存疑」不硬分 —— 資訊不足時不要猜。

兩種一定要擋掉的假陽性（實測在真實輸出的前 10 名裡抓到）：

- **組織變更**　`菜豚屋餐飲有限公司 → 菜豚屋餐飲股份有限公司`，字號完全一樣。
  這是同一家公司變更組織型態，當成換殼等於指控一家正常公司規避責任。
- **分支機構**　`香港商亞洲博聞有限公司台灣分公司 → 香港商亞洲英富曼會展有限公司台灣分公司`，
  外商在台分公司改名。分公司不是獨立法人，換不了殼。

另外 `pipeline/parse.py` 要擋**外國法人負責人**：`明鏡有限公司(薩摩亞商)`、
`鋐博生技股份有限公司(薩摩亞商 KST INVESTMENT LTD.)`。「薩摩亞商」是三個中文字、
不以公司結尾，不擋的話會變成一個叫這個名字的「人」，而且全台出現很多次
→ 憑空長出一堆假的跨公司關聯。⚠ 擋的時候不要把外籍負責人的姓名誤殺
（Vilhelm Robert Wessman、David James Webster 都要留著），所以只列公司型態字。

**自我檢查**：程式最後會驗已知大集團有沒有漏進換殼名單，
檢查名單用**公司名**（法人）不用自然人姓名，因為這份檔案會進 git。
這一項失敗就代表規則有洞，不要用那份輸出。

## 人工標註（T6）的三條規矩

1. **標註檔裡不能有系統的判斷。** 分數、分級、`why` 一律不放。
   標註者看到「分數 12.3、A 級」就會傾向同意機器，一致率虛高，數字沒有意義。
   `make_review.py` 把分層答案另外寫到 `link_review_key.csv`，標完才能看。
2. **兩個人各自獨立標同一份，中間不討論。** 討論過再標＝量同一個判斷兩次。
3. **分層抽樣，不要只抽證據最強的。** 只抽最像的會讓兩個人都答「是」，
   kappa 因為缺乏變異而失真。`QUOTA` 刻意留 15% 給「多半是同名巧合」那一層。

kappa 低不代表誰標錯，是**規則書沒寫清楚**。要做的是改規則書然後重標，
不是改資料、也不是改答案去湊。目標 0.61 以上。

## 這個 repo 是公開的

`tests/fixtures/` 的公司名與人名**全部是合成的**。原始樣本是真實裁處公告，
但把幾百個真人姓名連同「違反勞動法令」推上公開 repo，跟政府依法公告是兩回事，
而且 **git 歷史事後刪不乾淨**。

假名化的作法是**逐字元替換、保留所有結構特徵**：字數、括號位置、全形半形、
「即」字、巢狀括號、合夥負責人寫法、罕用字（含 Unicode 增補平面）、
原住民姓名的分隔點、日籍姓名、外文姓名、去識別化的 OO、機構字尾。
替換前後的分類分布比對過，一致（85/63/18/17/10）。

fixture 測的是**解析器怎麼處理各種形狀**，不是特定的某個人。

`.gitignore` 用「預設全擋 `data/*`、白名單放行」而不是一條一條擋，
因為漏擋一次就是把個資推上去，而且 `data/gcis/` 583 MB、`records.csv` 201 MB
都超過 GitHub 的單檔上限，漏擋會讓 push 直接失敗、要重寫歷史才能救。

`pipeline/shell.py` 與 `tests/test_shell.py` 裡的統一、遠東等**公司名刻意保留** ——
那是法人不是自然人，而且是用來驗證「已知大集團有沒有被擋在換殼名單外」的檢查名單。

### ⚠ 遮罩要遮的是「能回查的鍵值」，不是「看得懂的字」

`web/src/data/lookup.sample.json` 曾經只遮公司名與人名，
**統一編號、完整門牌、處分字號原封不動留著**，而那個檔案是進 git 的。
檔頭還寫著「已去識別化」。

那三個欄位都是一步就能回查的鍵值：

    統編     → 商工登記 → 公司全名、負責人本名
    完整門牌  → 商工登記 → 同址的所有公司
    處分字號  → 勞動部查詢系統 → 公司全名、負責人本名

商工登記與裁處公告本身是公開資料，這些欄位不是秘密。問題在於我們把
**「這個負責人涉嫌換殼」這個本系統明確拒絕做的認定**，
跟一把可以直接查到本人的鑰匙放進同一個檔案。
而且宣稱已去識別化，比誠實地不遮更糟 —— 那是假的安全感。

現在 `anonymize=True` 時：統編清空、處分字號遮號碼、地址只留到路名。
**留路名是刻意的** —— 「同地址」這項證據要看得出來，而路名查不到特定公司。

規矩：**遮罩前先問「這個欄位能不能查回本人」，不是「這個欄位看不看得懂」。**

## 備註欄不等於訴願欄（實測）

備註欄有 14,039 筆非空值、**557 種**寫法，直接塞進 `appeal` 會出事：

| 值 | 筆數 | |
|---|---:|---|
| `0` | 7,672 | 佔位值，畫面上會變成「訴願：0」 |
| 訴願駁回 | 1,325 | 已確定 |
| 職業災害 | 613 | **跟訴願無關** |
| **行政救濟中** | **494** | **本案尚未確定** |
| **訴願中** | **214** | **本案尚未確定** |

`pipeline/export.py` 的 `appeal_of()` 只認訴願／行政救濟字樣，認不出來一律回 `None`。

**爭議中的案子在畫面上要標「尚未確定」的徽章，不是寫在小字裡。**
原處分還沒確定，把它跟已確定的案子混在一起呈現，對被列的公司不公平，
對使用者也是誤導。這是紅線不是體貼。

## 紅線

這個系統處理**真實公司與真實人名**。誤判是實質的名譽損害。

| ❌ 不能輸出 | ✅ 只能輸出 |
|---|---|
| 這家公司有高度違法風險 | 本公司負責人 王○明，另擔任 △△公司負責人。該公司於 2021/03/15 因違反勞基法第 24 條遭裁罰 20,000 元。資料來源：勞動部公告〔連結〕 |
| 我們抓出虛設行號／違法企業 | 我們產生待查訊號／呈現公開紀錄的關聯 |

- 每一句都要對應一筆官方公告、附連結、**不加形容詞**
- Demo、截圖、影片一律去識別化
- 利用備註欄的訴願結果：只呈現「訴願駁回」或無訴願紀錄者，爭議中的明確標註
- **如果我寫的文案或程式輸出出現「認定」語氣，主動指出來**

---

## 交接（隊友的前置條件，延誤她就動不了）

| 最晚 | 給什麼 | 她拿去做 |
|---|---|---|
| 9/09 | 專案骨架推上 GitHub（本 repo） | T1 環境設定、T5 地圖頁 |
| 9/12 | `parse_review.csv` 200 筆 | T3 標註 |
| **9/25** | **M0 衝刺結果 + 三張圖** | **T4 報名文件（10/1 交，硬截止 10/5）** |
| 10/12 | `mock.osha.json` 換真資料（格式不變） | T5 收尾 |
| 10/14 | `link_review.csv` 100 組 + `severity_review.csv` 150 筆 + 標註規則書 | T6 / T7（10/20 截止） |
| 10/20 | 完整資料集清單（名稱、網址、機關、期間、筆數、具體用途） | T8 資料使用度說明 |
| 10/26 | 可操作的系統 | T9 測試、T11 影片 |
| 10/31 | 評估數字（kappa、合理率、ablation、誤判分析） | T10 簡報 |

**那 100 組配對我也要獨立標一次，不能跟她討論。** kappa 是兩人的一致率。

---

## 目錄

```
common.py               Windows 主控台編碼修正（每個進入點第一行呼叫）
gcis/constants.py       商工登記的欄位事實與遮罩問題
gcis/fetch.py           下載 g0v 打包檔（610MB，可中斷續抓）
gcis/load.py            打包檔 → data/gcis.duckdb（entity / director 兩張表）
crawler/constants.py    30 單位 × 10 法規 的表單代碼、五組表格欄位（實測抄下來的）
crawler/mol.py          爬蟲。打 /Download/ 拿完整 CSV，原樣存檔，順便記涵蓋範圍
pipeline/parse.py       合併欄位解析（五種格式，實測 43,437 筆）
pipeline/dedupe.py      同一處分重複（處分字號差一字母）
pipeline/exclude.py     排除公部門與大企業
pipeline/build.py       raw → records.csv（印出的多公司數字是上界，不能用）
pipeline/signal.py      同名多公司分級 + 同名巧合檢定 ← M0 Q1 真正的答案在這
pipeline/rarity.py      姓名稀有度 → 可信度分數，並自我驗證分數有沒有效
pipeline/shell.py       換殼／集團分流，把大企業擋在換殼名單外
tests/test_shell.py     分類規則的測試（改規則一定要跑）
pipeline/join.py        勞動部 × 商工登記對接，產生地址與登記狀態的佐證
pipeline/geo.py         違規聚合到鄉鎮市區（依**登記地址**，不是發生地點）
tools/geocode.py        商工登記地址 → 經緯度（要連 github，本機 Linux VM 連不到）
pipeline/refresh.py     自動更新：重抓一輪、跟上一輪比對、報出新增／消失／訴願變更
tools/make_review.py    產生人工標註檔（link_review / parse_review）
tools/kappa.py          算兩人標註的 Cohen's kappa
tests/test_parse.py     用真實邊角樣本測解析
tools/make_mock.py      前端假資料產生器
web/src/types/contracts.ts   資料契約（唯一型別來源）
web/src/pages/OshaMap.tsx    隊友的檔案，我不要動
```

---

## 查詢頁的版面（Claude Design 的設計稿）

`web/src/styles/tokens.css` ＋ `web/src/styles/home.css` ＋ `web/src/pages/Home.tsx`。

**行動優先。** 兩個 CSS 檔的預設值就是 375px 的樣子，桌機的兩欄只在
`@media (min-width: 980px)` 裡加上去。反過來寫（桌機當預設、手機當例外），
手機上會一直有橫向捲軸而且沒有人會發現 —— 這個專案到現在為止
**每一次檢查都是在 1920 寬的螢幕上做的**。

**顏色只有三個有意義。** `--fatal`（涉及死亡災害）、`--pending`（行政救濟
尚未確定）、`--link`（可以點）。設計稿裡沒有任何純裝飾用的顏色。
⚠ `--fatal` 跟 `--pending` **不可以併成同一個色相**：
「死了人」是嚴重度，「還不確定」是不確定性，用同一個顏色就是把兩種意思
講成一種。加第四個顏色之前先回答「它代表什麼」。

**⚠ 不要加回 Google Fonts 的 `<link>`。** 理由跟當初拿掉 OpenStreetMap
圖磚一樣：正式站不打別人的伺服器。設計稿原檔有那一行，實作時拿掉了。
中文襯線字刻意**不排 PMingLiU（新細明體）** —— Windows 只有新細明體與
標楷體兩種襯線中文字，兩種在 13px 都糊得看不清楚，寧可讓 Windows
退回無襯線。設計稿的性格主要來自版面的線條與等寬數字，那兩者每個平台都有。

**數字一律 `.num`（等寬＋`tabular-nums`）。** 比例字寬的「1」比「8」窄，
兩欄數字對照時欄位會歪掉。

### 這一版動到的資料語意

**證據強度不再顯示分數。** 只顯示「幾項獨立佐證」＋完整的佐證句子。
一個「0.75」會被讀成「75% 是同一人」，而那個數字本來就不是機率。
分數還在資料裡（拿來排序），只是不進畫面。

⚠ 算獨立佐證時，`same_name`（姓名相同）與 `rare_name`（姓名罕見）
**都不算**。兩者講的是同一件事：這個姓名。罕見只讓「同名巧合」的機率變低，
它沒有提供第二個獨立線索 —— 把它算成一項就是把同一個訊號數兩次，
正是「身分訊號與樣態訊號不可相加」要擋的事。

**處分字號從 `content` 裡拆出來變成獨立欄位 `doc_no`。**
以前是串在違反內容後面的「（處分字號 ○○）」。串在一起的結果是：
欄位對不齊、使用者沒辦法只複製字號、長清單想只顯示字號也做不到。

**⚠⚠ 法條與違反內容不可以配對。** 兩個欄位常常是好幾項用「;」串起來的，
攤成一行一項可以讀，但**不可以把第 1 條法條配第 1 段內容**。
實測 2,657 筆裡有 135 筆是多項的，其中 **44 筆兩邊的項數對不起來** ——
分隔符號在來源資料裡就不一致（「;」「、」換行都有，而且「；」會出現在
同一項的句子中間）。配錯就是把 A 法條掛到 B 事實上，直接顯示在一家
真實公司的紀錄裡。各自斷行不主張任何對應關係，所以安全。

**設立年份是西元，頁面上其他日期是民國。** 商工登記的 `established`
是 `"1989"`。不標示的話「設立 1989」會被讀成民國 1989 年。
**標示，不換算** —— 換算是多一次可能出錯的轉手，標示零成本。

### 撞名選擇一定要給得出區別

`choose` 回的是 `Candidate[]`（統編／登記現況／地址／設立／筆數），
不是名稱字串。實測「國城營造有」對到的兩家裡有**名稱完全相同**的 ——
只列名稱的話使用者看到兩行一模一樣的字，等於逼他亂猜。
每家要各抓一片（候選之間雜湊不同），所以有 `DESCRIBE_LIMIT = 24` 的上限。

### 長清單（>12 筆）

年度分面 ＋ 篩選 ＋ 一次 12 筆。國城營造 397 筆，整串倒出來沒有人看得完，
而且會把「有幾筆涉及死亡災害」這種真正重要的事淹掉。
⚠ 「依年度」的筆數多寡受檢查與稽核頻率影響，不是「那一年比較危險」，
這句話一定要寫在旁邊。
⚠ 只有「全部」一個分面時不要畫分面列，一顆按不動的按鈕只是雜訊。

### 已驗證的資料事實（畫面上有寫，改文案前先重跑）

- 9 部法規、民國 100–115 年；110 年以後佔 88.7%（562,200 / 633,861）。
- **基隆市與新竹市的職業安全衛生法公告一筆都沒有**（兩市只有勞基法、
  性平法、勞退、最低工資、中高齡就業）。
- 台北市的職安法公告有 16,536 筆「罰鍰 0」＝公告未載金額，不是免罰。

---

## 自動更新（`pipeline/refresh.py`）

決賽的「資料使用度」占 30%。抓一次做一個網站是「用了資料」；
**持續追蹤同一批公告的生滅與變更**才是「把資料用起來」。

```
python -m pipeline.refresh --baseline    # 第一次：把現在的 data/raw 存成基準快照
python -m pipeline.refresh               # 重抓 → 比對 → 出報告（不動 data/raw）
python -m pipeline.refresh --promote     # 檢查過了才換上去
```

**⚠ 重抓不可以直接蓋掉 `data/raw`。** 一輪 300 次請求、約半小時，中間可能
斷線、被擋、網站改版。直接蓋的話，抓壞的那一刻同時失去舊資料與比對能力。
所以固定流程是：抓進 `data/raw_new/` → 比對 → 檢查 → promote（舊的移到
`data/raw_prev/`）。`crawler.mol.crawl()` 的 `dest` 參數就是為了這件事加的。

**⚠ 筆數變少一律當成可疑，不是當成事實。** 某一組（單位×法規）這次比上次少，
可能是真的下架，也可能是這次抓壞了。程式分不出來，所以 `--promote` 會直接
擋下來，要人看過報告再加 `--accept-shrink`。
「查詢系統少報違規紀錄」是這個專案最不能犯的錯，寧可多擋幾次。

**⚠⚠ 第 0 欄「編號」不可以參與比對。** 它是結果集裡的流水號（1..N）。
中間插進一筆新公告，後面每一筆的編號全部往後移 —— 把它算進內容雜湊，
新增 1 筆會被報成「43,436 筆內容變更」，整份報告當場報廢。
實測過：造一份「刪 3 筆、改 2 筆備註、加 1 筆並重編全部編號」的檔案，
程式正確報出「新增 1、消失 3、變更 3」，沒有一筆誤報。

### 怎麼認出「同一筆公告」

來源沒有主鍵。識別碼 = 單位代碼＋法規代碼＋**處分字號＋事業單位名稱＋處分日期
＋違反法規條款**＋組內出現序號。

⚠ 識別碼**刻意不含罰鍰、備註、法條敘述** —— 那些欄位會被更新（訴願結果就住在
備註欄）。含進去的話，一筆公告的訴願狀態改變會被報成「舊的消失了、新的出現了」，
正好把最有價值的訊號丟掉。

⚠ 副作用要老實寫：實測職安署×職安法 43,437 筆裡，有 84 筆（0.19%）連
「全部欄位」都完全相同。那些列靠組內序號區分，所以該組增減一筆時序號會移位，
可能誤報成一筆消失＋一筆新增。報告的結尾有寫這一段。

### 產出

| 檔案 | 內容 | 進 git？ |
|---|---|---|
| `data/refresh/{日期}/report.md` | 人看的報告 | ❌ 有真實公司名 |
| `data/refresh/{日期}/changes.csv` | 新增／消失／變更明細 | ❌ 有真實公司名 |
| `data/snapshots/{日期}.csv.gz` | 歷史索引（只有 key 與 digest） | ❌ 每份約 13 MB |
| `data/refresh_log.csv` | 每輪的統計，**只有數字** | ✅ 已在 .gitignore 放行 |

`refresh_log.csv` 是簡報要用的那一張：它證明這個系統不是一次性的抓取。

### 第一次真跑（2026-09-14，距離上一次抓取 12 天）

```
660,311 → 661,563（+1,252）
新增 1,256（其中 2 是同一處分重新公告，實際新的處分 1,254）
消失 4　內容變更 18（全部都是訴願狀態）
沒有任何一組筆數異常縮水
```

三個從真實資料學到的事：

**一、⚠ 公告日期要放進識別碼。** 華膳空廚 `府勞檢字第1150136647號` 在 115/07/09
公告過，115/09/08 又公告一次，**其餘欄位一字不差**，而且新的那列被插在舊的前面。
公告日期不在識別碼裡的話，會被報成「seq 0 的公告日期變了」＋「seq 1 新增」——
看起來像兩件事，其實只有一件。放進去之後乾淨地報成「新增一列」。

**二、同一個處分會被重新公告，那不是新的違規。** 全庫 661,563 列裡有 318 組
（多出 333 列，0.05%）是同一個字號＋處分日期＋公司＋法條、只有公告日期不同。
報告會把新增拆成「新的處分」與「重新公告」兩個數字。
（下游 `pipeline/dedupe.py` 本來就會合併掉，上線筆數沒有被灌水；
拆開只是為了讓報告的數字誠實。）

**三、「訴願中」會變回空白，而那不等於原處分維持。** 實測有三筆從「訴願中」
變成空白（長榮航空、鈞安婦幼、嘉倍管顧）。備註清空可能是訴願結束，但**駁回還是
撤銷看不出來**。`export.py` 只看得到當下的備註，所以標示會整個消失 ——
畫面等於默默告訴使用者這案子確定了。已處理：`refresh.py` 把這些處分字號寫進
`data/appeal_cleared.csv`，`appeal_of()` 讀它並顯示

> 本案曾公告訴願中，現行公告未載訴願狀態；結果未公開

⚠ 這一類**不掛「尚未確定」標籤**（掛了等於說案子還在進行，它可能早就結束了），
也**不可以不顯示**（不顯示等於說沒有人爭議過）。所以查詢頁用中性樣式
（`.vio-appeal-plain`，灰線不是琥珀色）把事實寫出來。
⚠ 備註若又變回訴願中、或寫出結果，`refresh.py` 會把紀錄**移除** ——
留著會讓畫面同時說「訴願駁回」跟「結果未公開」，自相矛盾。

### 還沒做

- 排程本身（Windows 工作排程器或 GitHub Actions）還沒設。
- promote 之後要重跑 `build → parse → join → publish`，目前是手動。
- `data/appeal_cleared.csv` 含真實公司名，**不進 git**。換電腦或重建環境時
  它不會跟著過去，那些案子會退回「沒有訴願標示」。目前沒有補救機制。

---

## T3 人工標註（欄位解析）：整體 kappa 不要引用，要拆開

2026-09-14 兩人各自標完 200 筆抽樣：

```
整體                     n=200  一致 156/200（78.0%）  kappa 0.231
  系統有拆出結果           n=150  一致 150/150（100%）   kappa 1.000
  系統說「拆不開」          n= 50  一致   6/50 （12.0%）  kappa 0.000
```

**⚠ 整體那個 0.231 沒有意義，不要寫進任何文件。** 它把一個滿分的精確率
跟一個定義爭議平均成一個中間值。

分歧 44 筆**全部**是「拆不開」，而且方向完全一致（一個全標錯、一個大多標對）。
那不是標註品質問題，是**題目對那一類沒有定義**：「拆得對嗎」沒說「系統沒拆出
東西」算什麼。一個人讀成「系統老實承認拆不開，判斷正確」，另一個讀成
「這其實拆得開，沒拆就是錯」。兩種都站得住。

真正的毛病是把兩件事混在一題問。要分兩題：

1. **精確率** —— 系統有輸出時，事業單位／負責人拆得對嗎？
2. **召回率** —— 系統說拆不開時，這筆其實拆得開嗎？

文件要引用的是拆開後的數字：
> 系統有輸出的 150 筆，兩名標註者一致率 100%（kappa 1.000），其中 3 筆
> 判定拆解錯誤，正確率 98.0%。

`python -m tools.kappa a.csv b.csv --by 型態` 會自動拆。

### 這次標註抓到的 bug（值得寫進報名文件）

追下去發現 `_MULTI` 會比對「及」，而
「呂淑貞記帳**及**報稅代理業務人(呂淑貞)」裡的「及」是**業別名稱的一部分**，
不是「甲及乙」的連接詞 —— 整筆被判成「多名雇主」丟掉。

```
全庫 388 筆拆不開：
  記帳及報稅代理（人／業務人）  327 筆（84.3%）  ← 這個 bug
  括號沒配對／被截斷             45 筆
  真的多名雇主等                 16 筆
修正後：388 → 61
```

⚠ **只補這一類。** 剩下的 34 筆「括號沒配對」照舊拒絕 ——
缺右括號的還原方式不唯一（公司名到哪為止？），猜錯不是少一筆，
是把**錯的人名**放進連結圖。

⚠ 測試裡有一筆特別重要：`林愷明記帳及報稅代理業務人(林承旺)` ——
名稱裡的人跟括號裡的人**不是同一個**。負責人一定要取括號裡的。

---

## 名稱（2026-09-14 定案）

```
隊名    職得調查
主題    職得調查——以負責人姓名為軸線之違反勞動法令紀錄關聯查詢系統
標語    面試前，把老闆的名字查一遍
```

諧音「值得調查」。「職」呼應投稿組別（LaborOD 職安視覺化防災及素養提升組）。

⚠ **標語不是裝飾，是在修主標的語氣。** 「調查」在中文隱含一個調查者與一個
被調查的對象，單獨出現會靠近「這家公司有嫌疑」——那跟「本系統不對任何
事業單位或個人作出評價或認定」互相打架。標語把動作的主詞拉回**求職者自己**、
對象拉回**名字**（不是公司）。封面與報名表凡是出現主標的地方，標語都要跟著。

⚠ 站上目前還叫「換殼追蹤／求職安全雷達」（`index.html` 的 title、
`App.tsx` 的 brand、`Home.tsx` 的 hero）。**還沒改，要一起改。**
