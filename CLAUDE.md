# CLAUDE.md — 換殼追蹤

2026 第 31 屆 InnoServe。主投 LaborOD（勞動部職安署），副投 GCIS-OPENDATA（經濟部商發署）。
報名截止 10/05 · 初賽 10/13–23 · 決賽 11/07。

---

## 這個專案在做什麼

勞動部的違法雇主查詢系統**以公司為單位**。老闆解散公司換名字重開，違法紀錄就留在舊公司。
我們把軸線換成「人」：從一家公司 → 它的負責人與董監事 → 這些人名下的其他公司 → 那些公司的違法紀錄。

**核心技術問題**：兩邊資料沒有共同識別碼。勞動部只有公司名與負責人姓名，**沒有統一編號**。
只能靠姓名對接，而「陳志明」在全台有 467 家公司。
連 g0v 的公司資料網站都在注意事項寫「若要確認是否為同一人請進一步至經濟部商業司查詢」。

**把這一步自動化並量化可信度，就是本專案的貢獻。**

---

## 工作規則（踩過的坑，不要重踩）

### git

- **不要幫我 push。** 改動放在資料夾裡，然後給我：① 一句話說明改了什麼 ② 要 `git add` 哪些檔案 ③ commit 訊息 ④ 要不要跑 migration。
- 查狀態用 `git --no-optional-locks status`。一般的 `git status` 會留下 `.git/index.lock`，而這個環境刪不掉，會擋住我的 `git add`。
- 不要用 `git add -An` 之類的 dry-run，一樣會留 lock。

### 密鑰

- `.env` **永遠不進 git**。`.gitignore` 要有 `.env` / `.env.*` 但保留 `!.env.example`。
- **密碼、API key、連線字串不要貼進對話。** 貼過就視為已外洩，立刻更換。
- 雲端環境變數填連線字串時**不要加引號**。

### Python 環境（Windows）

- **一定要用 venv。** 直接用系統 Python 的 `pip install`，在 Windows 上很容易裝到
  另一個直譯器去（Microsoft Store 版、Anaconda、多版本並存），然後
  `python -m crawler.mol` 跑起來說找不到 requests。
  ```
  cd D:\class-project\shell-watch
  py -3.12 -m venv .venv
  .venv\Scripts\activate
  python -m pip install -U pip
  pip install -r requirements.txt
  ```
  之後每次開新終端機都要先 `.venv\Scripts\activate`（提示字元前面會出現 `(.venv)`）。
- **Python 要 3.10 以上**（用了 `X | None` 的型別寫法）。
- **`python -m crawler.mol` 一定要在專案根目錄跑**，不然是 `No module named crawler`。
- **主控台編碼**：Windows 預設 cp950，印不出 `⚠` 這類符號會直接 UnicodeEncodeError
  掛在半路。每個進入點都呼叫 `common.use_utf8_stdout()` 擋掉了，新寫的進入點也要加。
- `.venv/` 不進 git。
- **HTTP header 不能放中文。** header 只能是 latin-1，放了中文會在
  `http.client.putheader()` 掛掉，訊息是 `'latin-1' codec can't encode characters`，
  完全看不出跟 User-Agent 有關。踩過一次了（`crawler/mol.py` 的 `USER_AGENT`）。

### 前端

- **驗證用 `npm ci`，不要用 `npm i`。** 用 `npm i` 可能裝到跟 lockfile 不同的版本，然後出現「本機好好的，部署就壞」。上一個專案在這裡爆過：本機是 @types/react 19，Render 的 `npm ci` 裝 18，`RefObject<T | null>` 型別不相容，build 直接掛。
- **不要在 Windows 專案資料夾裡從 Linux 跑 `npm install`。** rollup 有平台專屬的原生檔案，Linux 裝的在 Windows 上跑不動（反之亦然）。要驗證就在乾淨的暫存目錄跑。
- **hooks 絕對不能放在 early return 之後。** 兩次渲染的 hook 數量不同 → React error #310 → 整頁白屏，而且 TypeScript 檢查不出來。`eslint.config.mjs` 已經把 `react-hooks/rules-of-hooks` 設成 error 並納入 `npm run build`。
- **顏色一律用 `var(--xxx)`，禁止寫死色碼。** 唯一來源是 `web/src/styles/tokens.css`。
- **資料從 JSON import，不要寫死在元件裡。** 真資料上線時只換檔案。

### 資料

- 爬下來的原始檔**先原樣存檔**（`data/raw/{單位}_{法規}.csv`），不要邊爬邊處理。爬蟲掛掉時不用重來。
- **用 DuckDB，不要用 pandas 全載入。** g0v 打包資料 2.4 GB，解開後好幾 GB。
- ⚠ **但寫進 DuckDB 要用 DataFrame 批次，不要 `executemany` 一列一列塞。**
  DuckDB 是欄式分析資料庫，逐列 INSERT 是它最差的情況。實測 20,000 列：

  | 作法 | 速率 | 300 萬列推估 |
  |---|---:|---:|
  | PRIMARY KEY + executemany | 343 列/秒 | **146 分鐘** |
  | 拿掉 PRIMARY KEY | 1,490 列/秒 | 34 分鐘 |
  | 拿掉 PK ＋ DataFrame 批次 | 233,697 列/秒 | **13 秒** |

  差 680 倍。DuckDB 會把本地的 DataFrame 直接當表讀（replacement scan），
  所以 `INSERT INTO t SELECT * FROM df` 幾乎零成本。
  **PRIMARY KEY 也不要加** —— 去重在 Python 的集合做掉就好，
  PK 只是讓它每列再檢查一次唯一性。索引最後才建。
- ⚠ **查 DuckDB 只查需要的那幾筆，不要全表掃再丟進 Python。**
  `pipeline/refine.py` 第一版對 427 萬筆 entity 跑相關子查詢
  （`(SELECT count(*) FROM director WHERE id = e.id)`）再全部載入，
  被 OOM killer 砍掉 —— **exit 137，stdout 與 stderr 都是空的**，
  完全看不出發生什麼事。把需要的名單先寫進暫存表再 JOIN，差好幾個數量級。
  看到「什麼都沒印、exit code 137」就是記憶體被砍，不是程式沒跑。
- **長時間的迴圈要印進度。** 70 MB 的分片跑起來看起來就像當掉了，
  人會以為是壞了而中斷 —— 而它其實只是慢。
- 含真實姓名的資料**不進 git**（`.gitignore` 已擋 `*_review.csv`、`data/annotation/`）。

---

## 資料事實（2026-09-02 實地驗證，不是推測）

### 勞動部違反勞動法令查詢系統

`https://announcement.mol.gov.tw/`（2026-09-02 讀表單原始碼＋實際打過）

- **沒有 API**。但查詢與下載都是同一個 form 的 POST，可以直接打：
  - 查詢 `POST /` → HTML，五組表格各自分頁
  - 下載 `POST /Download/` → **CSV，完整結果，沒有分頁**
  → **不要爬 HTML 表格、不要處理分頁**，直接打 `/Download/`。這省掉九成的工。
- 必要參數：`_csrf_token`（先 GET `/` 拿）、`CITYNO`、`REGNUMBER`、`downloadType=3`
- **不能空查詢** → 按「單位 × 法規」跑迴圈（代碼表在 `crawler/constants.py`）
- **30 個單位**（後 8 個是中央機關與園區管理局，很容易漏）× **10 種法規** = 300 次
- ⚠ 偶爾回傳 **HTML 錯誤頁**，但狀態碼還是 200、content-type 還是寫 text/csv。
  **一定要驗內容**（`crawler/mol.py` 的 `_looks_like_csv`），重試就會過。
- ⚠ HTTP header 寫 `charset=ANSII`，是錯的，實際是 **UTF-8 with BOM**。
- ⚠ 下載檔的**欄位順序跟畫面不一樣**：畫面是「…處分字號、事業單位」，
  下載是「…事業單位、處分日期、處分字號」。事業單位在第 4 欄。以下載檔為準。
- ⚠ 每列資料的欄位數比標題列**多 1**（行尾多一個逗號）。
  用 `len(row) == len(header)` 過濾會把資料全部丟光。
- ⚠ 罰鍰金額是**帶千分位的字串**（`"20,000"`）。
- 日期：資料裡是 `115/08/05`，但查詢表單要填 `1150805`。兩種不一樣。

**畫面顯示的筆數 ≠ 下載到的筆數。**
台北市職安法畫面寫「最近 3 年 3,391 筆」，下載回來 **16,708 筆（103/10～115/08，12 年）**。
下載的是全部年份 —— 資料量比原本估的多 4 倍。

- 備註欄有**訴願結果**（例：「訴願駁回」）→ 可區分已確定與爭議中，是法律風險的重要防線

### ⚠ 職安法的地理欄位幾乎是空的（2026-09-02 實測，這件事改變了計畫）

職安法確實多三個欄位（罹災人數／發生日期／發生地點），但**填寫率極低**：

| 單位 | 職安法筆數 | 有發生地點 | 填寫率 |
|---|---:|---:|---:|
| 職業安全衛生署 | 43,437 | 92 | 0.2% |
| 桃園市 | 6,655 | 32 | 0.5% |
| 高雄市 | 5,825 | 2 | 0.03% |
| 新北市 | 4,474 | 30 | 0.7% |
| 台中市 | 3,269 | 43 | 1.3% |
| 台南市 | 600 | 3 | 0.5% |
| **台北市** | **16,708** | **0** | **0%** |
| 基隆市／新竹市 | 0 | — | 沒有任何資料 |

罰鍰金額也一樣不一致：桃園 99.8% 有填，台北市 **16,708 筆全部是 0**。

**後果：**
1. 「用發生地點畫職災地圖」做不出來 —— 全台可用的點大約只有 200 個。
   第三層退路（職安風險地圖）**不能靠這個欄位**。
2. 地圖要改用**雇主的商工登記地址**（g0v 有），標題要誠實寫
   「違法雇主登記地點」，不是「職災發生地點」。這對換殼追蹤反而更貼題。
3. 罰鍰金額**不能拿來跨縣市比較**，也不能當嚴重度指標。
   `pipeline/build.py` 把「0」與空值都當成「沒填」，不是「罰 0 元」。
4. **這個發現本身就是題材**：資料揭露品質的縣市落差，是可以寫進簡報的實證觀察。

### 商工登記

- 開發用 g0v 打包：`http://ronnywang-twcompany.s3-website-ap-northeast-1.amazonaws.com/index.html`
  （資料日期 2026/07/20）
  - `index.csv.gz` 50MB —— 只有三欄 `id,type,name`，是**名稱 → 統編的索引**，
    type 分「公司」「商業登記」「分公司」
  - `files/` 1.14GB —— 但 `.json.gz` 與 `.jsonl.gz` 是同樣內容的兩份，
    **只要抓 20 個 `.jsonl.gz`，共約 560MB**
    - 前 10 個是公司與分公司
    - `bussiness-` 開頭的 10 個是商業登記（獨資、合夥）
      ← **換殼追蹤最relevant的一群**，資本額門檻低、開關容易
  - `gcis-history/` 1.16GB、`tax-history/` 64MB —— 目前用不到

### 商工登記載入後的實測數字（2026-09-03，`data/gcis.duckdb`）

    entity 4,267,888 筆　director 1,776,537 筆　（101 秒）
      商業登記 2,256,861　公司 1,930,265　分公司 80,762

⚠ **狀態欄位有全形與半形兩種寫法**：「歇業／撤銷」123,598 筆、
  「歇業/撤銷」52,008 筆。只認一種會**靜靜地**把 17 萬筆歸到「還在營業」。
  `clean_status()` 已經統一成半形。

⚠ **停業 ≠ 解散。** 「核准停業」31,608 筆是暫時的，復業的公司不是換殼。
  `CLOSED_STATUS`（永久）與 `SUSPENDED_STATUS`（暫時）分開列，不要混。

⚠ **設立年份有 2821 這種值。** 不擋範圍的話「設立未滿 N 年」會整個歪掉。

⚠ **合夥商號的出資額是 `{姓名: 金額}` 的 dict**，不是字串。
  當成字串處理會把資本額丟掉；而且 key 就是合夥人名單，是免費的第二份來源。

未遮罩的 79,883 筆幾乎都是**分公司經理**（不是負責人），
所以「負責人姓名一律遮罩」這個結論成立。
- API：`/api/show/<統編>`、`/api/name?q=<姓名>&page=1`
- ⚠ **打包檔的姓名全部遮罩，API 沒有遮罩。** 這是 2026-09-02 才查清楚的，
  之前寫「沒有遮罩」是錯的 —— 那次是用 API 查台積電，不是看打包檔。

  | 來源 | 百爵產業有限公司(20386468) 的代表人 |
  |---|---|
  | S3 打包檔 | `黃_斌`（實測該分片 1,926 筆遮罩率 **100%**，有限／股份公司都一樣） |
  | g0v API | `黃哲斌` |

  **所以兩邊分工**：登記地址、資本額、設立日期、登記現況、所營事業用打包檔
  （沒有遮罩問題、一次抓完）；完整姓名用 API（有量的限制，只用在候選名單）。
  高價值的三件事 —— 地址佐證、大企業排除、公司是否已解散 —— **完全不需要 API**。

  遮罩的名字也不是廢的：「黃_斌」保留了姓與末字，
  拿來比對勞動部的「黃哲斌」是很強的部分驗證，而且免費。
- ⚠ **財政部的資料只給「負責人姓氏」**（例如「仝」），一律用經濟部的
- 官方 API 要 email 申請並**綁外部 IP**，有每日上限 → 開發用 g0v，正式文件引用官方

### 已知資料限制（寫文件時要誠實揭露）

1. **各縣市保存期間差很多**：台北市勞基法涵蓋 12 年以上，有些縣市不到 2 年，
   基隆市與新竹市的職安法**一筆都沒有**。
   那不是雇主比較守法，是資料沒公開。→ 跨縣市比較一律用「期間內的率」，
   依據是 `python -m crawler.mol` 產生的 `data/coverage.csv`。
2. **沒有統一編號**，只能靠姓名對接。
5. **欄位填寫率因縣市而異**（見上一節）。沒填 ≠ 0。
3. **無法確認身分**：公開資料沒有身分證字號。我們**不宣稱能確認**兩個「王小明」是同一人，
   只量測「這個連結有多少獨立佐證」。
4. **系統偏保守、會漏抓** —— 這是刻意的：漏抓一個案例，使用者少收到一個警示；
   誤指一個沒問題的雇主，是實質傷害一個人。兩種錯誤的代價差好幾個量級。

---

## 架構原則

### AI 負責理解與連結，規則負責判定

- **LLM 只用在一個地方**：違法情節的嚴重度分級（`違反法規內容` 是自由文字）。
- **解析用 regex 就夠**（`pipeline/parse.py`），不要用 LLM —— regex 可驗證、免費、可重現。
  用職安署 43,437 筆實測，雇主欄有**五種格式**，不是一種：

  | 格式 | 佔比 | 例 |
  |---|---:|---|
  | 公司(負責人) | 44.97% | 七福營造有限公司(官建昌) |
  | 只有機構名 | 44.64% | 脇酩安全工程有限公司 |
  | 某某即某商號 | 6.29% | 王宇傑即衍寶企業社 |
  | 自然人雇主 | 2.34% | 方信和 |
  | 各種邊角 | 1.76% | 見 `tests/fixtures/hard_employers.txt` |

  一開始只寫括號那一條，成功率 37%，看起來像資料很髒 —— 其實不是。
  而且**「即」字與自然人雇主這兩種對換殼追蹤最有價值**：
  獨資商號與自然人雇主本來就最容易換個名字重新登記。當成解析失敗丟掉，
  等於把最該追的對象丟掉。現在整體約 99.8% 拆得開，
  拆不開的（多名共同雇主、括號沒配對）一律進 `unparsed.csv` 交人工，不硬猜。

  ⚠ **踩過的坑：沒有姓氏表就分不出人名與店名。**
  「外省牛肉麵」「轉角鍋物」「街口飯糰」「醇品咖啡屋」都是 3–5 個中文字、
  不以「公司」結尾 —— 跟人名長得一模一樣。`外省牛肉麵(陳木元)` 因此被判成
  「負責人＝外省牛肉麵」的自然人雇主：真正的負責人被丟掉，而一個不存在的
  「人」進了關聯圖。真實資料裡這種假姓氏有 813 種。
  現在 `parse.py` 有姓氏表，**但只用在沒有括號的情況** ——
  括號裡的位置本身就說明它是負責人，那裡不查表，否則 奥田実（日籍）、
  禤惠儀（罕見漢姓）這些真的負責人會被誤殺。

  ⚠ **括號裡也可能是機構屬性。**「…老人長期照顧中心(養護型)」的「養護型」
  在全台出現幾十次，會被當成「同一人名下 13 家公司」的重大發現。

  兩種錯的代價不對稱，所以判不出來時一律當機構：
  多一個機構名 = 一筆紀錄的欄位怪怪的；多一個「人」 = 憑空長出假的跨公司關聯。
- **風險分數、門檻、要不要顯示，全部是規則。** 理由不是保守，是這件事會影響公司名譽，
  判斷過程必須逐條可稽核。這句話要寫進簡報。

### 兩階段，刻意降風險

```
階段一（保證做得出來）  違法名單內部 → 同負責人多公司 → 慣性違法雇主圖譜
階段二（加分，可能失敗）跨機關 join → 他「現在」還經營哪些公司
```

階段一先做完再碰階段二。最壞情況也一定有東西可以交。

### 三層退路

若 M0 衝刺發現換殼案例太少：

1. 換殼追蹤（跨部會）→ 2. 慣性違法雇主圖譜（單源）→ 3. 職安風險地圖（不需實體解析）

第三層其實最貼組別主題（職安署贊助、組名有「視覺化」）。**三層都用得到隊友的地圖頁。**

---

## 身分訊號 vs 樣態訊號（不要混在一起）

2026-09-03 用商工登記的**同地址**當獨立量尺，檢驗每個訊號實測出來的：

| 訊號 | 類型 | 同地址比例（符合 vs 不符合） | 倍數 | 顯著 |
|---|---|---|---:|:--:|
| 可信度分數 ≥ 3 | 身分 | 6.69% vs 2.06% | 3.24× | ✓ |
| 可信度分數 ≥ 5 | 身分 | 8.69% vs 3.40% | 2.55× | ✓ |
| 裁處單位都是同一縣市 | 身分 | 4.54% vs 3.05% | 1.49× | ✓ |
| 先前的公司都已停業 | 樣態 | 3.70% vs 3.79% | **0.98×** | － |
| 只有 2 家公司 | 樣態 | 3.83% vs 3.44% | 1.11× | － |
| 資本額都低於 100 萬 | 樣態 | 3.31% vs 3.88% | 0.85× | － |

**「先前的公司都已停業」是 0.98 倍 —— 完全沒有效果。**
第一直覺會說「這是換殼的定義性特徵，怎麼可能沒用」。
它確實有用，但**回答的是另一個問題**：

- **身分訊號**（姓名稀有度、同縣市、同地址）→ 這幾家公司是不是同一個人的
- **樣態訊號**（已停業、時間依序、組織型態）→ 這個樣子像不像換殼

同地址量的是身分，所以量不出樣態訊號的價值。而且全國本來就有 **55%**
的登記處於解散／歇業狀態，那個訊號接近基準率，對身分判斷沒有資訊量。

**排序要分兩層做：先用身分訊號決定連結可不可信，再用樣態訊號決定是換殼還是集團。**
把兩種訊號加進同一個分數，等於把兩個不同的問題混成一個答案。

## 最終排序是兩層（pipeline/rank.py）

身分層只用**驗證過**的身分訊號分級，樣態層沿用 `shell.py` 的分類。
分層只用「姓名稀有度」與「同縣市」，**同地址不參與分層**，
留下來當獨立的校準尺 —— 這樣校準才不是循環論證。
同地址本身是最強的單一證據（274 倍），所以它的作用是**直接把該組升到 A 層**。

實測校準（同地址比例）：

| 層級 | 組數 | 同地址 | ±2SE |
|---|---:|---:|---:|
| A 姓名罕見＋同縣市 | 220 | **11.36%** | 4.28 |
| B 姓名罕見 | 3,048 | 4.46% | 0.75 |
| C 同縣市 | 162 | 7.41% | 4.12 |
| D 兩者都沒有 | 5,230 | **1.40%** | 0.32 |

A > B 顯著、C > D 顯著、**B 與 C 分不出來**（C 只有 162 組，2SE 是 ±4 個百分點）。
所以能講的是 **A > {B, C} > D**，不要宣稱 B 一定強於 C。

⚠ **比較相鄰兩層一定要看樣本數。** 第一版寫成「比例沒有隨層級遞減就判定分層失敗」，
把 C 層的雜訊當成錯誤。這個坑在這個專案已經踩過三次
（refine.py 的門檻挑選、rarity.py 的分層判定、這裡），一律要跟 2SE 比。

⚠ `join.py` 的輸出**必須帶 `county_only`**。少了它，`rank.py` 分不出
「同一個縣市」與「兩家都是勞保局裁處」，A 層與 C 層會變成 0 組，而且不會報錯。

## 大企業排除：商工登記幫不上忙（實測結論）

用資本額、設立年、董監事人數做排除，**七個門檻沒有一個的效果超過 2 個標準誤**。
`pipeline/shell.py` 的四個訊號已經把大企業處理掉了，再篩沒有剩餘價值。

第一版的作法是「挑出讓同地址比例上升的條件」，會砍掉 51% 的候選，
換到 0.5 個百分點、而且在雜訊範圍內的「改善」—— 那是純粹的損失。
**同地址只有 3~4%，樣本一少比例本來就會跳動，一定要跟抽樣誤差比。**

兩個實作上的坑：
- `director` 表裡有「合夥人」（來自出資額 dict 的 key）。
  混著算，「董監事 ≥ 5 人」會去砍 21 萬資本額、9 個合夥人的小商行。
- 「設立滿 20 年」量的是**開了多久**不是**有多大**。
  實測被它砍掉的包括資本額 5 萬的餐飲企業社、100 萬的清潔公司 ——
  那正是我們要找的對象。

商工登記的價值在**佐證**（同地址 274 倍、登記現況），不在排除。

## 兩個絕對不能寫進簡報的數字

1. **build.py 的「名下有 2 家以上事業單位 N 個」** —— 那是**同名**的統計。
   「陳志明」在全台有 467 家公司。這個數字是上界，不是發現。
2. **build.py 的「各公司違法期間不重疊」** —— 稀疏資料下這幾乎是算術必然。
   兩家公司各只有一筆違法，只要不同天就一定「不重疊」。它不代表換殼。

能用的是 `pipeline/signal.py`：要求至少一家有 2 筆以上（span 是真的期間）、
兩段之間隔 90 天以上、名字 3 字以上，並且做**同名巧合檢定** ——
比較「這些名字的多家公司全在同一縣市」的實際比例與隨機期望值。
實際 >> 期望才代表訊號存在。

簡報能講的是「這批連結整體顯著偏離隨機（N 倍）」，
**不能講「我們找出了 N 個換殼案例」**。個別配對要人工標註 + Cohen's kappa。

## 可信度分數（pipeline/rarity.py）

把姓名拆成「姓 + 名」分別估機率再相乘，算出「照機率這個名字本來預期
會出現在幾家公司上（λ）」，再用 Poisson 上尾算實際看到 k 家有多不尋常：

    score = -log10( P(X ≥ k | λ) )

**為什麼要拆開姓與名**，有兩個理由，兩個都重要：
1. 直接數出現次數是循環論證 —— 我們要判斷的就是那幾次是不是巧合。
2. 拆開後，單一姓名對 P(姓)、P(名) 的貢獻只有幾萬分之一，可以忽略。

⚠ **踩過的坑：機率要用「每家公司一筆」估，不是「每個不重複姓名一筆」。**
   一開始寫成後者，P(名=明) 變成「有多少種名字含明」而不是「有多少人叫明」，
   常見姓名的 λ 被嚴重低估，於是「陳志明名下 29 家公司」被判成 99 分的
   重大發現 —— 而那正是 29 個不同的人。**分數把最不可信的組排到最前面。**
   λ = 公司數 × P(姓名)，P 是公司母體上的分布，估計就要在同一個母體上做。

⚠ Poisson 累加在 λ > 745 時 `math.exp(-lam)` 會下溢成 0，p 變 1.0、分數變 0，
   把該標注的組默默漏掉。常見姓名的 λ 真的會到幾百，所以 λ>500 改用常態近似。

**分數自己有沒有效，程式會驗**：分數越高的組，名下公司落在同一縣市的比例
應該越高。用合成資料（植入 1,500 個已知真案例）測過，趨勢是
6.8% → 8.5% → 13.7% → 15.5%，單調上升。**這張表就是簡報上「技術怎麼證明」那張圖。**

## 換殼 vs 集團（pipeline/shell.py）

rarity.py 的分數在量「這個關聯有多難得」，它做對了 —— 分數最高的幾組是
統一、遠東、統聯這些真實集團，系統在沒有任何先驗知識的情況下把它們拼了回來。
**但那是正確的辨識、錯誤的目標。** 求職者不需要知道統一超商的老闆也開統一企業，
而把徐旭東放在「求職安全雷達」首頁是災難級的誤導。

在接到商工登記（實收資本額、設立年份、董監事人數）之前，用四個手上就有的訊號分流：

| 訊號 | 集團 | 換殼 |
|---|---|---|
| 時間樣態 | 同時經營 | 先後接續 |
| 公司名 | 共用字根（統一〇〇、遠東〇〇） | 刻意改名，不相關 |
| 組織型態 | 股份有限公司 | 有限公司、企業社、工程行、獨資 |
| 公司數 | 8 家以上 | 2–4 家 |

判定用規則不用模型，每一條都要能說出理由（`why` 欄位）。
兩家都是股份有限公司又只有兩家時，判「存疑」不硬分 —— 資訊不足時不要猜。

兩種一定要擋掉的假陽性（實測在真實輸出的前 10 名裡抓到）：

- **組織變更**　`菜豚屋餐飲有限公司 → 菜豚屋餐飲股份有限公司`，字號完全一樣。
  這是同一家公司變更組織型態，當成換殼等於指控一家正常公司規避責任。
- **分支機構**　`香港商亞洲博聞有限公司台灣分公司 → 香港商亞洲英富曼會展有限公司台灣分公司`，
  外商在台分公司改名。分公司不是獨立法人，換不了殼。

另外 `pipeline/parse.py` 要擋**外國法人負責人**：`明鏡有限公司(薩摩亞商)`、
`鋐博生技股份有限公司(薩摩亞商 KST INVESTMENT LTD.)`。「薩摩亞商」是三個中文字、
不以公司結尾，不擋的話會變成一個叫這個名字的「人」，而且全台出現很多次
→ 憑空長出一堆假的跨公司關聯。⚠ 擋的時候不要把外籍負責人的姓名誤殺
（Vilhelm Robert Wessman、David James Webster 都要留著），所以只列公司型態字。

**自我檢查**：程式最後會驗已知大集團有沒有漏進換殼名單，
檢查名單用**公司名**（法人）不用自然人姓名，因為這份檔案會進 git。
這一項失敗就代表規則有洞，不要用那份輸出。

## 人工標註（T6）的三條規矩

1. **標註檔裡不能有系統的判斷。** 分數、分級、`why` 一律不放。
   標註者看到「分數 12.3、A 級」就會傾向同意機器，一致率虛高，數字沒有意義。
   `make_review.py` 把分層答案另外寫到 `link_review_key.csv`，標完才能看。
2. **兩個人各自獨立標同一份，中間不討論。** 討論過再標＝量同一個判斷兩次。
3. **分層抽樣，不要只抽證據最強的。** 只抽最像的會讓兩個人都答「是」，
   kappa 因為缺乏變異而失真。`QUOTA` 刻意留 15% 給「多半是同名巧合」那一層。

kappa 低不代表誰標錯，是**規則書沒寫清楚**。要做的是改規則書然後重標，
不是改資料、也不是改答案去湊。目標 0.61 以上。

## 這個 repo 是公開的

`tests/fixtures/` 的公司名與人名**全部是合成的**。原始樣本是真實裁處公告，
但把幾百個真人姓名連同「違反勞動法令」推上公開 repo，跟政府依法公告是兩回事，
而且 **git 歷史事後刪不乾淨**。

假名化的作法是**逐字元替換、保留所有結構特徵**：字數、括號位置、全形半形、
「即」字、巢狀括號、合夥負責人寫法、罕用字（含 Unicode 增補平面）、
原住民姓名的分隔點、日籍姓名、外文姓名、去識別化的 OO、機構字尾。
替換前後的分類分布比對過，一致（85/63/18/17/10）。

fixture 測的是**解析器怎麼處理各種形狀**，不是特定的某個人。

`.gitignore` 用「預設全擋 `data/*`、白名單放行」而不是一條一條擋，
因為漏擋一次就是把個資推上去，而且 `data/gcis/` 583 MB、`records.csv` 201 MB
都超過 GitHub 的單檔上限，漏擋會讓 push 直接失敗、要重寫歷史才能救。

`pipeline/shell.py` 與 `tests/test_shell.py` 裡的統一、遠東等**公司名刻意保留** ——
那是法人不是自然人，而且是用來驗證「已知大集團有沒有被擋在換殼名單外」的檢查名單。

## 備註欄不等於訴願欄（實測）

備註欄有 14,039 筆非空值、**557 種**寫法，直接塞進 `appeal` 會出事：

| 值 | 筆數 | |
|---|---:|---|
| `0` | 7,672 | 佔位值，畫面上會變成「訴願：0」 |
| 訴願駁回 | 1,325 | 已確定 |
| 職業災害 | 613 | **跟訴願無關** |
| **行政救濟中** | **494** | **本案尚未確定** |
| **訴願中** | **214** | **本案尚未確定** |

`pipeline/export.py` 的 `appeal_of()` 只認訴願／行政救濟字樣，認不出來一律回 `None`。

**爭議中的案子在畫面上要標「尚未確定」的徽章，不是寫在小字裡。**
原處分還沒確定，把它跟已確定的案子混在一起呈現，對被列的公司不公平，
對使用者也是誤導。這是紅線不是體貼。

## 紅線

這個系統處理**真實公司與真實人名**。誤判是實質的名譽損害。

| ❌ 不能輸出 | ✅ 只能輸出 |
|---|---|
| 這家公司有高度違法風險 | 本公司負責人 王○明，另擔任 △△公司負責人。該公司於 2021/03/15 因違反勞基法第 24 條遭裁罰 20,000 元。資料來源：勞動部公告〔連結〕 |
| 我們抓出虛設行號／違法企業 | 我們產生待查訊號／呈現公開紀錄的關聯 |

- 每一句都要對應一筆官方公告、附連結、**不加形容詞**
- Demo、截圖、影片一律去識別化
- 利用備註欄的訴願結果：只呈現「訴願駁回」或無訴願紀錄者，爭議中的明確標註
- **如果我寫的文案或程式輸出出現「認定」語氣，主動指出來**

---

## 交接（隊友的前置條件，延誤她就動不了）

| 最晚 | 給什麼 | 她拿去做 |
|---|---|---|
| 9/09 | 專案骨架推上 GitHub（本 repo） | T1 環境設定、T5 地圖頁 |
| 9/12 | `parse_review.csv` 200 筆 | T3 標註 |
| **9/25** | **M0 衝刺結果 + 三張圖** | **T4 報名文件（10/1 交，硬截止 10/5）** |
| 10/12 | `mock.osha.json` 換真資料（格式不變） | T5 收尾 |
| 10/14 | `link_review.csv` 100 組 + `severity_review.csv` 150 筆 + 標註規則書 | T6 / T7（10/20 截止） |
| 10/20 | 完整資料集清單（名稱、網址、機關、期間、筆數、具體用途） | T8 資料使用度說明 |
| 10/26 | 可操作的系統 | T9 測試、T11 影片 |
| 10/31 | 評估數字（kappa、合理率、ablation、誤判分析） | T10 簡報 |

**那 100 組配對我也要獨立標一次，不能跟她討論。** kappa 是兩人的一致率。

---

## 目錄

```
common.py               Windows 主控台編碼修正（每個進入點第一行呼叫）
gcis/constants.py       商工登記的欄位事實與遮罩問題
gcis/fetch.py           下載 g0v 打包檔（610MB，可中斷續抓）
gcis/load.py            打包檔 → data/gcis.duckdb（entity / director 兩張表）
crawler/constants.py    30 單位 × 10 法規 的表單代碼、五組表格欄位（實測抄下來的）
crawler/mol.py          爬蟲。打 /Download/ 拿完整 CSV，原樣存檔，順便記涵蓋範圍
pipeline/parse.py       合併欄位解析（五種格式，實測 43,437 筆）
pipeline/dedupe.py      同一處分重複（處分字號差一字母）
pipeline/exclude.py     排除公部門與大企業
pipeline/build.py       raw → records.csv（印出的多公司數字是上界，不能用）
pipeline/signal.py      同名多公司分級 + 同名巧合檢定 ← M0 Q1 真正的答案在這
pipeline/rarity.py      姓名稀有度 → 可信度分數，並自我驗證分數有沒有效
pipeline/shell.py       換殼／集團分流，把大企業擋在換殼名單外
tests/test_shell.py     分類規則的測試（改規則一定要跑）
pipeline/join.py        勞動部 × 商工登記對接，產生地址與登記狀態的佐證
tools/make_review.py    產生人工標註檔（link_review / parse_review）
tools/kappa.py          算兩人標註的 Cohen's kappa
tests/test_parse.py     用真實邊角樣本測解析
tools/make_mock.py      前端假資料產生器
web/src/types/contracts.ts   資料契約（唯一型別來源）
web/src/pages/OshaMap.tsx    隊友的檔案，我不要動
```
