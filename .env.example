# 這個檔案只放佔位字，不放真值。真的值寫在 .env，而 .env 永遠不進 git。

# 語言模型（只用在「違法情節嚴重度分級」一個地方）
LLM_API_KEY=your-key-here
LLM_MODEL=

# 經濟部商工行政資料開放平臺
# 注意：官方 API 要 email 申請（opendata.gcis@gmail.com）而且會綁「外部 IP」，
# 本機開發與雲端部署是兩個 IP，這是個坑。
# 開發階段建議用 g0v 的打包資料，正式文件再引用官方來源。
GCIS_API_KEY=

# 資料庫（部署時才需要）
DATABASE_URL=
